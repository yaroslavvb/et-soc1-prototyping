{
  "name": "et-soc1-prototyping",
  "private": true,
  "description": "Build-time tools only: scripts/build-report.py renders the reports' TeX to SVG with mathjax-full (scripts/tex2svg.js). Install with `npm ci`.",
  "devDependencies": {
    "mathjax-full": "3.2.1"
  }
}
