# Artifacts: what was published, and where it lives

Three kinds of artifact came out of this work: **published reports** (HTML, deployed to spacesheep.dev),
**repostable images**, and **tools** that can be re-run. Commits are listed at the end so any artifact can be
tied to the state of the tree that produced it. Cite as **A1**...**A19** (A9 and A10 were never assigned):
A1–A5, A11, A13–A15, A17 and A19 are reports · A6–A8 images · A12 math rendering · A16 the rerun tools · A18 the
wire tools. The public index of every report is the hub,
[Limits of observability](https://spacesheep.dev/@yaroslavvb/et-soc1-limits-of-observability#reports) (A2).

Reports A2–A5, A11, A13–A15 and A17 are assembled by `scripts/build-report.py <name> <data.json> <out.html>` from
`docs/reports/sources/<name>.{meta.json,body.html,script.js}` (A2 also keeps its data in
`limits-of-observability.data.json`). Edit the sources, not the HTML: it is regenerated. A1 is generated by
`python3 workloads/memprobe/build_report.py docs/reports/data/2026-09-19-memprobe-aifoundry2/summary.json
docs/reports/2026-09-19-et-soc1-memory-anatomy.html` from `workloads/memprobe/report_template.html`: edit the
template, not the HTML.
A19 and the four 2026-09-18 reports are hand-written HTML whose embedded JSON is rewritten by their `--embed`
scripts. The 2026-09-18 test drive (`docs/report/index.html`) and the two 2026-09-22 briefs are plain hand-written
HTML. For these, edit the prose in the HTML.

**Visibility.** Every space the hub links is public (Q40); the one exception, the David Kanter notes, is private and
no longer linked (below). This file is the record: after any deploy, compare `npx spacesheep list` with the
visibility columns here.

---

## Published reports

| ID | Report | Built from | Space | Visibility (2026-09-24) |
|---|---|---|---|---|
| A1 | `docs/reports/2026-09-19-et-soc1-memory-anatomy.html` — one memory access by stage and rail; generated by `workloads/memprobe/build_report.py`. **Revised 2026-09-24** after the review (the DRAM leg split on closed rows, 25 + 66 cycles; the model's error counts; the evict-ladder reading; the gap between the rail trace and the host log explained; links, heading anchors, a contents list and Related reports), rebuilt from `workloads/memprobe/report_template.html` | E1, E2 | [et-soc1-memory-anatomy](https://spacesheep.dev/@yaroslavvb/et-soc1-memory-anatomy) `2bf74fd1-fd7f-4e19-8e35-6168ae42657c` | public (Q40) |
| A2 | `docs/reports/2026-09-20-et-soc1-limits-of-observability.html` — what can be seen, from board power to a flip-flop. **Second edition (2026-09-23):** the hub for all measurement matters, with a table of contents, the index of every measurement report, the power meter chain, the unmetered remainder attributed, the Moortec sensors, the DDR-rail droop meter, the meter starved by s ↔ s+16 rings, and the improvement ladder (20 rungs). **Update (2026-09-24):** A17 in the index, E31–E32 in the sessions, the poisoned management queue and the stray write in §4.1 and rung 7; tables scroll inside their frames on phones. **Update (2026-09-24):** A19 in the index. **Revised 2026-09-24** after the review: titled "Limits of observability · ET-SoC-1 reports hub"; the index grouped by subject (energy and power; contention and moving data; memory and compute baselines; briefs), newest first within each group; a glossary (`#terms`); the fit described as over configuration means; the per-shire voltage map credited to A3; the method section says the scouts and reviewers were AI agents; the David Kanter notes no longer listed or linked (now private) | R1, R2, R3, R13, E2, E30 | [et-soc1-limits-of-observability](https://spacesheep.dev/@yaroslavvb/et-soc1-limits-of-observability) `2ea37420-67b9-484e-9d4c-581e8a9f0323` | public (Q40) |
| A3 | `docs/reports/2026-09-20-et-soc1-power-temperature.html` — every way to measure power and energy, and a load step through all of them | E5, E6 | [et-soc1-power-temperature](https://spacesheep.dev/@yaroslavvb/et-soc1-power-temperature) `acee5c6d-56c0-45e7-aa97-ce11af37bdd8` | public |
| A4 | `docs/reports/2026-09-20-horace-experiment.html` — the data-dependent power experiment, the flip model, the long runs, the structured matrices, the second card (**six versions**, see below) | E7–E17, E20 | [et-soc1-horace-experiment](https://spacesheep.dev/@yaroslavvb/et-soc1-horace-experiment) `da445a93-7be3-42c2-b9be-4992fa4a3b62` | public |
| A5 | `docs/reports/2026-09-21-why-low-power.html` — Esperanto's equation measured term by term against an A100 | E15, E10, R6, R7, R8 | [et-soc1-why-low-power](https://spacesheep.dev/@yaroslavvb/et-soc1-why-low-power) `baede20c-57d9-4e01-8157-2014670dd8cf` | public |
| A11 | `docs/reports/2026-09-22-dvfs-leakage.html` — six claims about DVFS loops and leakage suppression, from a conversation with David Kanter (MLCommons; R9, private notes), checked against the firmware, the RTL and the card | R9, R3, R2, E10, E18, E19, E20, E21 | [et-soc1-dvfs-leakage](https://spacesheep.dev/@yaroslavvb/et-soc1-dvfs-leakage) `171dcd4a-5b6d-49d3-aca0-db4980fabfa5` | **public** (the repo owner made it public on 2026-09-22 and asked that it stay so) |
| A13 | `docs/reports/2026-09-22-hot-line.html` — one contended global atomic: fair to the shire that hosts it, and fatal to that shire's own memory path | R11, R1, E22, E23 | [et-soc1-hot-line](https://spacesheep.dev/@yaroslavvb/et-soc1-hot-line) `ac439287-4503-42c7-88e7-b5d3e3b64b06` | public (Q40; it quotes a Discord message) |
| A14 | `docs/reports/2026-09-22-on-chip-relay.html` — handing a stage's output to the next shire by ID (3.5 mesh hops away on average) instead of DRAM: 12× faster at a twelfth of the energy. Its `docs/reports/data/2026-09-22-onchip-aifoundry2/onchip.json` was regenerated on 2026-09-24 by the current `analyze_onchip.py`: the rail averages now take the `[avg]` element (the file predated that fix and had never been re-run), and it adds `power.*.die_c` and `distance[].mesh_hops`; board power, pJ/B, rates and `onchip.txt` are unchanged | R12, E24, E25 | [et-soc1-on-chip-relay](https://spacesheep.dev/@yaroslavvb/et-soc1-on-chip-relay) `8678d49d-3f0c-49be-b08a-5528de8ece3c` | public (Q40) |
| A15 | `docs/reports/2026-09-23-energy-manual.html` and `docs/energy-manual/*.md` — the energy manual: at rest, awake, per instruction, per byte, between shires, synchronisation, composition, two cards, method. **Second edition** adds every instruction (3.1) and the fine grain: wires, lines, rows, the rail split and the SRAM leakage (4.3). **Third edition (2026-09-23):** every entry with a confidence bar (mean [lo–hi] over passes and cards, per-card mean ± se), the levels and rings re-measured on both cards at 600 MHz, the unmetered remainder attributed and the DDR droop meter (4.3; `docs/energy-manual/04a-fine-grain.md`). **Update (2026-09-24):** 4.3's wire difference is described as flips plus ones carried, with a link to A17 | E26–E30 and every earlier E through its builder | [et-soc1-energy-manual](https://spacesheep.dev/@yaroslavvb/et-soc1-energy-manual) `cc3cb1d6-51cf-420b-a165-7d8629904d97` | **public** (the repo owner asked for it on 2026-09-23) |
| A17 | `docs/reports/2026-09-24-heat-per-mm.html` — heat per millimetre: what moving a bit a millimetre across the mesh costs, split into bits that differ between flits, ones carried and a fixed part; free against shared links; against Dally's ~100 fJ/b-mm. Published after an adversarial review by a workflow of six AI agents (`docs/reports/data/2026-09-24-wire-energy/review/`) | R14, E31, E32 | [et-soc1-heat-per-mm](https://spacesheep.dev/@yaroslavvb/et-soc1-heat-per-mm) `5602ff62-878f-4db6-b703-02061000d9ce` | public |
| A19 | `docs/reports/2026-09-18-et-soc1-ridge-points.html` — "Ridge points · ET-SoC-1": peak compute divided by each level's bandwidth, i.e. the operations per byte a kernel needs before compute rather than memory sets its speed, with what that takes for blocked matmul and for batch sizes, the limits that act like ridge points, energy balance points and an A100 comparison. Derived from the four 2026-09-18 reports, with energies from A15, and no card time; computed 2026-09-18 (Q43), published 2026-09-24. Energy balance points recomputed on 2026-09-24 from the energy manual's data (A15). Unlike the reports above it is not built by `scripts/build-report.py`: the prose lives in the HTML and `scripts/ridge-points.py --embed` rewrites only its embedded JSON | the 2026-09-18 measurements (matmul, memory hierarchy, on-chip communication, sparsity), and, for the energy balance points and the bandwidth check, `docs/reports/data/2026-09-23-energy-manual/manual.json` and the 2026-09-23 reruns | [et-soc1-ridge-points](https://spacesheep.dev/@yaroslavvb/et-soc1-ridge-points) `dd341b0b-a52e-4e23-b8b3-101a83119133` | public (asked for on 2026-09-24) |

### A4's six versions

A4 was rewritten six times as the question got sharper. The file name never changed, so **only the latest is
on disk**; the earlier ones survive in git history and in the space's version list.

| Version | Commit | What changed |
|---|---|---|
| 1 | `df89c94` | E7: the effect exists, with a fitted temperature correction |
| 2 | `04746a3` | E8: every run started at 80 °C |
| 3 | `f2b8776` | E9–E11: strict control, heating per FLOP, the RTL activity model, the cool-die speed effect, the first GIFs |
| 4 | `07f7d04` | E12–E16: ten-minute runs, the flips-to-temperature model, structured matrices priced before they ran |
| 5 | `75bb061` | E17: fit and prediction separated, held-out validation |
| 6 | `9df563e` | E20: the same experiment on aifoundry3; the model transfers up to one scale factor; equations rendered as SVG |

## Other spaces the hub links (not built by this line of work)

| Report | Source in this repository | Space | Visibility (2026-09-24) |
|---|---|---|---|
| Matmul efficiency (R4, 2026-09-18) | `docs/reports/2026-09-18-et-soc1-matmul-efficiency.html` | [et-soc1-matmul-efficiency](https://spacesheep.dev/@yaroslavvb/et-soc1-matmul-efficiency) `590752c1-17a8-4f5d-97ef-bcf33fd6a3e7` | public (Q40) |
| Memory hierarchy (R4, 2026-09-18) | `docs/reports/2026-09-18-et-soc1-memory-hierarchy.html` | [et-soc1-memory-hierarchy](https://spacesheep.dev/@yaroslavvb/et-soc1-memory-hierarchy) `4b6e0a37-808d-4fc9-8001-555125733c46` | public (Q40) |
| On-chip communication (R4, 2026-09-18) | `docs/reports/2026-09-18-et-soc1-on-chip-communication.html` | [et-soc1-on-chip-communication](https://spacesheep.dev/@yaroslavvb/et-soc1-on-chip-communication) `ab8e1b2b-de17-44f4-8645-006fa960e349` | public (Q40) |
| Sparse compute (R4, 2026-09-18) | `docs/reports/2026-09-18-et-soc1-sparsity.html` | [et-soc1-sparse-compute](https://spacesheep.dev/@yaroslavvb/et-soc1-sparse-compute) `5abf6014-8de0-4e82-8744-5676bac6453e` | public (Q40) |
| Test drive (2026-09-18) | `docs/report/index.html` | [et-soc1-testdrive](https://spacesheep.dev/@yaroslavvb/et-soc1-testdrive) `16732875-c03e-434d-a643-7a432586c7f7` | public |
| Spatial temperature brief (2026-09-22) | `docs/reports/2026-09-22-et-soc1-spatial-temperature-brief.html`, imported from the published page on 2026-09-24 | [et-soc1-spatial-temperature-brief](https://spacesheep.dev/@yaroslavvb/et-soc1-spatial-temperature-brief) `bc391cfe-64e2-4f36-884c-9bbfcb267de8` | public (Q40) |
| L2 mainline starvation brief (R12, 2026-09-22) | `docs/reports/2026-09-22-et-soc1-l2-mainline-starvation.html`, imported from the published page on 2026-09-24 | [2026-09-22-et-soc1-l2-mainline-starvation](https://spacesheep.dev/@yaroslavvb/2026-09-22-et-soc1-l2-mainline-starvation) `49ca367f-886f-4c0a-b472-12d8fc449300` | public (Q40); kept and corrected in place, with an Update box pointing to A13, which re-measured its headline |
| Notes of a conversation with David Kanter (R9, 2026-09-20) | none | `f3533740-5ad9-45e1-927c-098dbbe5c210`, unlinked | **private** since 2026-09-24, the repo owner's decision: a personal memo quoting a private conversation. It had been made public with the other hub links (Q40) and was listed in the hub; every link to it has been removed. A11 and R9 describe the source in words |

## A12 — Math rendered at build time

`scripts/tex2svg.js` turns the TeX in a report body into standalone SVG, called from
`scripts/build-report.py`: display math between `$$`, inline math between `\(` and `\)`, anything inside
`<pre>` or `<code>` left alone. It needs `npm install --no-save mathjax-full` once.

**Why not the usual runtime MathJax:** the first attempt loaded MathJax from `cdn.jsdelivr.net`, and the
publishing host blocks external scripts in viewers with a content-security policy — the deploy says so. Every
equation would have shown as raw TeX to every reader. Pre-rendering also means the files stay self-contained
and the math is identical in every browser. MathJax colours its SVG with `currentColor`, so dark mode works
with no extra rule.

## Repostable images

| ID | File | Public address | Content |
|---|---|---|---|
| A6 | `docs/reports/horace-heating.gif` (0.46 MB) | `https://da445a93-7be3-42c2-b9be-4992fa4a3b62.spacesheep.app/horace-heating.gif` | Five runs each of zeros, ones and random normal: die temperature over 7 s, each run launched at the same temperature. Raw sensor steps plus the recovered curve |
| A7 | `docs/reports/horace-heating-6.gif` (0.91 MB) | same host, `/horace-heating-6.gif` | The same for six kinds of matrix |
| A8 | `docs/reports/horace-long.gif` (0.29 MB) | same host, `/horace-long.gif` | Log time axis: how long each workload takes to drive the die from 80 to 90 °C |

Each has a `.png` poster beside it. They are generated by `tools/ettelem/make_heating_gif.py` and
`make_long_gif.py` with PIL, at 2× and downsampled, one global palette.

They live in A4's space, so **A4 must always be deployed as a folder** (`index.html` plus the three GIFs and their
posters). On 2026-09-24 a single-file redeploy of A4 dropped them and broke their public addresses; they were restored
the same day by redeploying the folder.

## Tools (all under `tools/ettelem/` unless noted)

| Tool | What it does |
|---|---|
| `ettelem` (C++, `tools/ettelem/ettelem.cpp`) | Telemetry client on `libDM.so`. `sample` emits JSON lines at up to 45 Hz: board power, the PMIC's per-rail running averages (as the service processor forwards them) and voltages, die and PMIC temperatures, clocks. `config` reads the governor's static inputs: the flashed TDP, the software temperature threshold and the power state (E21). Also `loglevel` and `sptrace`. **Holds the management node open**, which is single-opener |
| `etcfg` (C, `tools/etcfg/etcfg.c`) | The driver's `ETSOC1_IOCTL_GET_DEVICE_CONFIGURATION` as JSON: TDP, boot clock, shire mask, cache sizes. Read-only, no management node, so it works on a machine whose `libDM.so` refuses the card (E21) |
| `compare_cards.py` | Two cards' strict sessions side by side, with the model's predicted switching power for each pattern. Fits nothing (E20) |
| `run_hotline_power.sh`, `analyze_hotline_power.py` | Board power and rails while one atomic line is contended, against the same work spread over 32 (E23) |
| `workloads/nocbench --test hotline` | Many-to-one contention on one global atomic word: per-shire shares in a common window, the host shire's own traffic, the requester and pacing sweeps (E22, E23) |
| `workloads/nocbench/run_hotline.sh`, `analyze_hotline.py` | The 42-configuration sweep and its analysis |
| `workloads/onchip` | `--test probe`: can one shire read what another wrote into its scratchpad, and by which write path (E24). `--test relay`: the same multi-stage computation with its intermediate in DRAM, in the shire's own scratchpad, or in the next shire's (E25). Every run verifies its own output |
| `workloads/onchip/run_onchip.sh`, `analyze_onchip.py` | The 88-configuration sweep and its analysis |
| `run_onchip_power.sh` | Board power per medium, one twelve-second burst each, sized so the card is busy 83% of the time |
| `workloads/enercat` | The instruction and byte energy catalogue: one pattern flat out on every hart, 56 configurations, `run_enercat.sh` and `analyze_enercat.py` (E26) |
| `build_energy_manual.py`, `render_energy_manual.py`, `render_catalogue.py` | Assemble every table of the manual from its data file into one JSON, and render the markdown from it, so no number on the page is typed by hand |
| `workloads/enercat/gen_ops.py` | Generates the kernel's 174 instruction cases and the host's mode table from one instruction list (E27) |
| `workloads/enercat/run_catalogue.py`, `analyze_catalogue.py` | The 386-configuration, three-pass shuffled catalogue and its reduction: per-configuration statistics, the wire fit, the rail split, the SRAM leakage (E27, E28) |
| `ettelem sample --reset-ms` | Resets the service processor's rail statistics on a schedule and tags each sample with its window, for window means instead of filtered averages |
| `build_cards_data.py` | Assembles the three-machine block and merges it into both reports' data (E20, E21) |
| `run_horace_strict.sh` | The strict-start protocol, shuffled blocks, 7 s runs (E9) |
| `run_horace_cold.sh` | Runs from a cool die, for the governor (E10) |
| `run_horace_long.sh` | Long runs with a temperature cap and a watchdog (E12, E16) |
| `run_ablation.sh` + `ablation.cfg` | Strict-start runs of arbitrary `sparsity_host` configurations (E15) |
| `run_vf_cold.sh` | A careful voltage/frequency comparison. **Written, never run** |
| `make_tiles.py` | Structured 16×16 operand pairs (E13) |
| `analyze_horace_strict.py`, `analyze_horace_long.py`, `analyze_horace_cold.py`, `analyze_ablation.py` | Per-run and per-pattern analysis |
| `flip_thermal_model.py` | Fits the flips → power → temperature model (E17) |
| `validate_flip_model.py` | Tests a **frozen** model on data it was not fitted to; fits nothing (E17) |
| `predict_heat.py` | Prices a custom workload from its operand tiles: flips, watts, heating curve, time to a cap, sustainable duty cycle |
| `compact_telemetry.py` | Shrinks a telemetry log ~70× for storage in the repo |
| `finish_horace.sh` | Rebuilds the Horace line from the stored raw data: every analysis of E9–E17, the model, the validations, the GIFs, the E20 second-card block (`build_cards_data.py`), and the Horace and why-low-power pages |
| `rtl-sim/fma_toggle/` | The Verilator bench that counts switching activity (E11, E13) |
| `rtl-sim/pmu_carry/` | The Verilator bench that reproduces the counter bug (E2) |
| `analyze_dvfs.py` | Classifies every governor transition against the firmware's thresholds (unattributed where neither fired on the sampled values), computes the long idle's duration from timestamps (`--since`), counts aifoundry3's governor log events (`--sptrace`), and assembles the DVFS report's data (fits nothing) |
| `workloads/memprobe/gen_ops.py wakeup` | The array wake-up probe (E18) |
| `workloads/sparsity/host/main.cpp` | Extended in this work with `--values` (operand patterns, including `file:<path>` for custom tiles), `--dump-tiles`, `--stop-file` and fp16/int8 value fills |

## A16 — The rerun and attribution tools (2026-09-23)

- `tools/ettelem/analyze_reruns.py`: reduces every rerun pass with bracketing idle and the leakage correction,
  drops bursts whose clock left 600 MHz or whose sampler was starved, pools per card and over cards.
- `tools/ettelem/run_reruns_warm.sh`, `run_rl_warm_a2.sh`: passes preceded by heating the die past 76 °C.
- `tools/ettelem/run_rings_levels_power.sh`: the nocbench rings and memhier levels sampled by ettelem.
- `run_onchip_power.sh`, `run_hotline_power.sh`: the sampler now retries until it produces a line.
- `tools/ettelem/render_energy_manual.py`, `render_catalogue.py`, `build_energy_manual.py`: bars everywhere,
  the reruns and `unmetered_fit.json` carried into `manual.json`; `render_catalogue.py` writes the attribution
  and droop sections of 04a.
- `docs/reports/sources/limits-of-observability.{body.html,data.json,script.js,meta.json}`: the report now builds
  with the shared `scripts/build-report.py` like every other; the old `scripts/build-observability-report.py` was removed in `8084699`.
- `tools/ettelem/fit_unmetered.py` (added 2026-09-24): the unmetered-power attribution and the DDR droop fit of E30,
  from `catalogue.json` and the aifoundry2 catalogue telemetry. `unmetered_fit.json` was first computed inline and keeps
  those numbers; the script reproduces the attribution exactly and the droop coefficient to within 3% (0.87 against
  0.84 mV/W), and writes its own fit only with `--out`.

## A18 — The wire tools (2026-09-24)

- `workloads/enercat/run_wire.py`: both runs of the heat-per-mm experiment (E31 `--set v1`, E32 `--set v2`): the
  fills, shuffled passes bracketed by idle, `marks.jsonl` for the prefill and heater windows, and a sampler that is
  retried, drained with `dev_mngt_service` on a failed start, and restarted if it stalls.
- `workloads/enercat/analyze_wire.py`: the per-burst reduction (board power with the leakage correction, the mesh
  rail), per-pass slopes against distance, the model s0 + a·2P(1−P) + b·P, the complement test, the link-disjoint
  flows, the axes, per mm; and the report's checks (link sharing from the recorded reader>target maps under XY and
  YX routing, readers, per-reader bandwidth, the four-hop point off the line, the exit step, the 256 B excess) and
  the model refitted without the leakage correction (`--no-leak-correction` for the whole reduction).
- `tools/ettelem/build_wire_report.py`: joins `wire.json` with the sourced inputs (die, pitch, NoC, literature, first
  principles) into `report.json`; `tools/ettelem/chain_wire_v2.sh` starts the second run when the first is done.
- `workloads/enercat` host and kernel: `--pattern tstore_raw` (stores the host's bytes exactly) and `tstore_uniq`
  (every 512 B block unique, two regions per target), operand kinds `bern:P`, `alt:N`, `uq:P`, `frz`, options
  `--hop-axis`, `--pairs`, `--uniq-regions`, `--dump-slice`, the target map in every run record, and the guard that
  refuses a memory pattern with no slice.
- `tools/ettelem/ettelem.cpp`: `sample` finishes its request and exits on SIGTERM or SIGINT, so stopping it no longer
  poisons the management queue; `run_onchip_power.sh`, `run_hotline_power.sh` and `run_rings_levels_power.sh` drain
  the queue on a failed start.
- `docs/reports/sources/heat-per-mm.{body.html,script.js,meta.json}`: the report; every number in its sentences is
  computed from `report.json`.

## A19 — The ridge-point tool (computed 2026-09-18, published 2026-09-24)

- `scripts/ridge-points.py`: reads the raw data of the four 2026-09-18 reports (`docs/reports/data/*/runs.jsonl`,
  `results.json`) and their embedded JSON, checks the bandwidths against the 23 September reruns of the same probe on
  both cards, takes the energies per FLOP and per byte from the energy manual's data
  (`docs/reports/data/2026-09-23-energy-manual/manual.json`, since the review of 24 September), and prints every ridge
  point, the clock elasticity of each level, the limits that act like ridge points, the energy balance points and the
  A100 comparison. `--embed <html>` rewrites
  the report's `ridge-data` JSON; running it twice leaves the page byte for byte identical. Every spec constant in
  it (port and bank widths, LPDDR4X rates, PCIe, clocks) names its source in a comment. No card time.

## Cross-links between the reports

- **2026-09-24, afternoon (`d04b29a`).** Every report got the byline clause "· part of the measurement reports",
  linking the hub (A2). The six source-built pages whose committed HTML predated the template of the time
  (dvfs-leakage, horace-experiment, hot-line, on-chip-relay, power-temperature, why-low-power) were patched in both
  `<name>.body.html` and the built page rather than rebuilt; the two briefs were patched on the published page only.
- **2026-09-24, evening.** The review of the whole set added a "Related reports" list and a link back to the hub's
  list of reports to every page, anchors on every section heading, a contents list on pages with six or more
  sections, and GitHub links for repository paths. See the next section.

## The 24 September review

On the evening of 24 September 2026 every page the hub linked was reviewed for consistency, cross-links, suspicious
claims and readability by a workflow of AI agents. Each finding was verified against the raw data, the firmware or
the RTL before it was applied, and the corrected numbers were recomputed from the files in this repository. What
the review changed:

**Every page was revised.** Each keeps its space and slug. "Text in" names where the corrected text lives
(`sources/` is `docs/reports/sources/`); the built or edited HTML is the file in the tables above.

| Page | Space | Text in | Main corrections |
|---|---|---|---|
| Limits of observability, the hub (A2) | `2ea37420-67b9-484e-9d4c-581e8a9f0323` | `sources/limits-of-observability.*` | titled "Limits of observability · ET-SoC-1 reports hub"; index grouped by subject; a glossary; the unmetered fit is over configuration means (1.1–1.3 W rms on DRAM ones); the rails' average is the PMIC's; the voltage map credited to A3; the AI agents disclosed; the Kanter notes unlisted |
| The energy manual (A15) | `cc3cb1d6-51cf-420b-a165-7d8629904d97` | `sources/energy-manual.*`, `manual.json` (built by `build_energy_manual.py`) | the L1-fill figure (a unit error made it 32× too small); mesh energy grows with distance; the levels' method; the relay check re-priced; the awake core's floor; `fit_unmetered.py` cited |
| Heat per millimetre (A17) | `5602ff62-878f-4db6-b703-02061000d9ce` | `sources/heat-per-mm.*` | the DRAM comparison priced like for like (8.3 nJ a line; the hand-off 4.5× cheaper); a vector lane against a scalar add; terms; anchored links |
| The DVFS loop and its leakage (A11) | `171dcd4a-5b6d-49d3-aca0-db4980fabfa5` | `sources/dvfs-leakage.*`, `dvfs.json` | the governor runs once per ~133 ms pass; it hunts at the thermal threshold; the RTL is the open Erbium configuration, not the chip; seven down-steps unattributed; the idle computed from timestamps (20.6 h); the firmware-build caveat; Kanter paraphrased |
| Power and temperature (A3) | `acee5c6d-56c0-45e7-aa97-ce11af37bdd8` | `sources/power-temperature.*` | a Later measurements box; the load step's slope computed from its data (0.80 W/°C); the rails are the PMIC's running average (τ ≈ 1 s); all 34 shires on the voltage map |
| The Horace experiment (A4) | `da445a93-7be3-42c2-b9be-4992fa4a3b62` | `sources/horace-experiment.*`, `report.json`; rebuilt by `tools/ettelem/finish_horace.sh`; deployed as a folder with the GIFs | the long-run table joined to the right model records; 10 of 14 structured matrices within 0.6 W; fewer cores buy far more than proportional time; the leakage law extrapolated 7–14 °C |
| Why is it low power? (A5) | `baede20c-57d9-4e01-8157-2014670dd8cf` | `sources/why-low-power.*`; rebuilt by `finish_horace.sh` | the A100 compared at matching precision (bf16 5.4× better than fp32 here, 2.6× than fp16); its clock 1,160–1,410 MHz; three operating points; clock-gated, not gated off |
| One hot line stops a shire (A13) | `ac439287-4503-42c7-88e7-b5d3e3b64b06` | `sources/hot-line.*`, `hotline.json` | fair to within half a percent; the one-per-shire run is still saturated; the errata read with a hedge; the reruns' energies |
| Hand it to the next shire (A14) | `8678d49d-3f0c-49be-b08a-5528de8ece3c` | `sources/on-chip-relay.*`, `onchip.json` | the next shire is the next by ID, 3.5 mesh hops on average; distance costs energy, not bandwidth; mesh hops per offset computed |
| Anatomy of a memory access (A1) | `2bf74fd1-fd7f-4e19-8e35-6168ae42657c` | `workloads/memprobe/report_template.html` | the DRAM leg split on closed rows (25 + 66 cycles); the model's error counts; energies marked superseded by A15 §4 |
| Memory hierarchy | `4b6e0a37-808d-4fc9-8001-555125733c46` | the HTML | bandwidth from the 600 MHz launches; energies from A15 §4; DRAM latency per clock; an Update box |
| On-chip communication | `ab8e1b2b-de17-44f4-8645-006fa960e349` | the HTML (`analyze.py --embed` rewrites only its data) | a Later measurements box; the energy manual's reference bars (2.51 / 6.65 / 122 pJ/B); terms; an unsupported claim about marty1885's results removed |
| Matmul efficiency | `590752c1-17a8-4f5d-97ef-bcf33fd6a3e7` (must stay public) | the HTML | the A100 verdict qualified (±1/±2 operands; 2.9× on random data; the A100's bf16 tensor cores 5.4× better); per W above each workload's own idle |
| Sparse compute | `5abf6014-8de0-4e82-8744-5676bac6453e` | the HTML | a Later measurements box; the operands of the power runs named; the masked-load and shared-queue claims corrected |
| Ridge points (A19) | `dd341b0b-a52e-4e23-b8b3-101a83119133` | the HTML, and `scripts/ridge-points.py` for its data | energy balance points from A15's data; the tables split; the notation H/e |
| Test drive | `16732875-c03e-434d-a643-7a432586c7f7` | `docs/report/index.html` (deployed as the `docs/report` directory) | terms; "Next (as planned on 18 September)"; related reports |
| Spatial temperature brief | `bc391cfe-64e2-4f36-884c-9bbfcb267de8` | the HTML | imported; a tile is about 3.7 mm; the voltage map credited to A3; raw LaTeX replaced; no sideways scroll on phones |
| L2 mainline starvation brief | `49ca367f-886f-4c0a-b472-12d8fc449300` | the HTML | imported; an Update box pointing to A13 (the 6% owner penalty was not reproduced); the errata reading; no sideways scroll on phones |

- **The two briefs imported.** The L2 mainline-starvation brief and the spatial-temperature brief existed only as
  published pages. They were imported as `docs/reports/2026-09-22-et-soc1-l2-mainline-starvation.html` and
  `docs/reports/2026-09-22-et-soc1-spatial-temperature-brief.html`: standalone HTML, corrected there, with the byline
  link, a related list and the footer. Deploy them from those files.
- **The David Kanter notes made private and unlinked.** Space `f3533740-5ad9-45e1-927c-098dbbe5c210` is private
  since 2026-09-24, the repo owner's decision. The hub and every report dropped their links to it; A11, R9 and
  [16-dvfs-and-leakage.md](16-dvfs-and-leakage.md) paraphrase his statements, with no quotation marks.
- **The Horace GIFs restored.** A single-file redeploy of A4 had dropped A6–A8 and broken their public addresses;
  redeploying the folder restored them the same day. A4 is deployed only as its folder.
- **The shared template** (`docs/reports/sources/report.template.html`) gained a contents list for pages with six or
  more sections, the footer link "← All ET-SoC-1 measurement reports" to the hub's list, GitHub links for
  repository paths written as inline code, and a `pre code` style rule. The source-built pages were rebuilt with it;
  the standalone pages carry the same two scripts pasted before `</main>`. A1's template carries the hub byline.
- **Scripts and data changed with the pages:** `tools/ettelem/analyze_dvfs.py` (`--since`, `--sptrace`, unattributed
  down-steps) and `dvfs.json`; `workloads/onchip/analyze_onchip.py` (mesh hops) and `onchip.json` (A14 row);
  `workloads/memhier/analyze.py`'s `point_ghz` (a 10 ms threshold, the inferred clock kept within 600–800 MHz);
  `scripts/mmbench-power.py` (each workload's own idle, `idle_before_w`); `scripts/ridge-points.py` (reads
  `manual.json`); `tools/ettelem/build_horace_report_data.py` (each long run's model record); the errata text in
  `hotline.json`; the energy manual's builders and `manual.json`; and the new `tools/ettelem/fit_unmetered.py` (A16).
- **The findings files** mirror their pages, and the claim index, the register and this file record the corrected
  numbers.

## Commits

| Commit | Subject |
|---|---|
| `76085a3` | Take one ET-SoC-1 memory access apart, by stage and by power rail |
| `beb262c` | Survey the limits of observability on the ET-SoC-1 |
| `584b9a6` | Add observability tooling: flame graphs, PMU carry root cause, counter syscall |
| `df89c94` | Measure power and temperature; reproduce the Horace matmul experiment |
| `04746a3` | Rerun the Horace experiment with every run started at 80 °C |
| `f2b8776` | Horace experiment, third version: strict control, RTL flip model, thermal chain, GIF |
| `07f7d04` | Long runs, a flips-to-temperature model, structured matrices, and why the chip is low power |
| `75bb061` | Separate fit from prediction in the flips-to-temperature model |
| `17a1aa5` | Add docs/findings: a self-contained, traceable write-up of the ET-SoC-1 work |
| `b36acd6` | Check Kanter's DVFS and leakage-suppression claims against the ET-SoC-1 |
| `9df563e` | Run the experiment on a second card, and find why the third is slow |
| `e5715db` | One hot line stops a shire |
| `24fc7bf` | Hand it to the next shire: on-chip relay against main memory |
| `a10cb93` | Read the compacted telemetry in the on-chip analysis |
| `6377ce4` | The ET-SoC-1 energy manual, first edition |
| `4845a60` | Energy manual, second edition: every instruction on two cards, and the fine grain |
| `8084699` | Energy manual, third edition: confidence bars; observability, second edition: the unmetered remainder |
| `626e981` | Record the commits in the artifacts index |
| `8fa8ede` | Complete the commit table in the artifacts index |
| `d0e12d9` | Observability: sessions section, related documents, wider tables, shareable anchors; manual chart headroom |
| `1006df4` | Observability: no horizontal overflow in the tables at any width |
| `e7951c3` | Related reports: a described section in both reports instead of a footnote |
| `a7ecb2d` | Heat per millimetre: the mesh's wire energy per bit·mm, measured and reviewed |
| `b31b98a` | Record the heat-per-mm commit in the artifacts index |
| `8d17612` | Derive the ET-SoC-1's ridge points: the reuse each memory level demands |
| `6f8fba9` | Index the ridge-points report in the observability hub |
| `d04b29a` | Link every report back to the observability hub |

## Publishing notes, learned the hard way

- Deploy with `npx --yes spacesheep deploy <file-or-dir> --space <uuid> -m "<message>"`. **Always pass
  `--space`** when updating, or the CLI creates a new space.
- **Deploy A4 as its folder, never as a single file.** The GIFs (A6–A8) are part of that space, and a single-file
  deploy drops them (it happened on 2026-09-24; the folder was redeployed the same day).
- A folder deploy (`index.html` plus assets, which is how A4 ships its GIFs) has **changed a space's
  visibility in both directions**. Run `npx spacesheep list` after every deploy and fix it with
  `npx spacesheep share <uuid> --visibility public|private`. The visibility column above is a snapshot, and
  visibility is the repo owner's call, not the deploy script's.
- **Delete any `.spacesheep.json` the CLI leaves behind, every time.** It pins a folder to a space, and
  `deploy` uses it when `--space` is absent. On 2026-09-22 a stale `docs/reports/.spacesheep.json` from the
  DVFS brief silently published the hot-line report **over** that brief. The recovery is to remove the pin
  file, redeploy the right HTML with an explicit `--space`, and check the published `<title>`. Safest habit:
  deploy a new report from a directory of its own, and always pass `--space` for an update.
- A new space is private and gets its slug from the file or folder name. Pass `--title`, `--slug`, `--emoji`
  and `--description` on the first deploy, or fix them on the next one; all four are kept on later updates.
- **On an update, `--title` without `--slug` re-slugs the space.** On 2026-09-24 the review's redeploy passed
  new titles and every one of the 18 spaces moved to a slug made from its title (the old URLs went to a sign-in
  page) for about seven minutes, until each was redeployed with `--slug <old slug>`. Pass the existing `--slug`
  whenever you pass `--title`, and check `spacesheep list` for the URLs afterwards.
