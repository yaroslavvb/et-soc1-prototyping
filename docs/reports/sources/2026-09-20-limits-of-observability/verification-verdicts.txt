
##  | refuted=False | status->needs_tooling
CLAIM: 
REASON: The cited mechanism is real and the quotes are exact: external/core-et/dv/standalone/cpu_subsystem/vsrc/top.sv lines 391-416 gate $dumpfile("trace.fst")/$dumpvars() (Verilator) or $vcdplusfile/$vcdpluson (VCS) on +dump=1 / +traceEnable=1, csrc/Vtop_main.cpp:89 sets traceEverOn(true), and `define TIMING at top.sv:4 makes the 1 GHz / 200 MHz forever-loop clocks at lines 187-195 active. Status needs_tooling is correct, not works_now: (1) dv/standalone/common/ contains only common_cc.mk and vcs.mk; the verilator.mk that Makefile line 20 includes when VERILATOR=1 is absent from the drop, so the Verilator path does not build as shipped and the required --trace-fst/--timing flags are nowhere in the
CORRECTED: Bit-level waveform capture exists in the core-et standalone bench: +dump=1 or +traceEnable=1 (top.sv:391-416) calls $dumpfile("trace.fst")+$dumpvars() under Verilator (traceEverOn(true) in Vtop_main.cpp:89) or $vcdplusfile/$vcdpluson under VCS, at 0.5 ns resolution (1 GHz TIMING clock, top.sv:4,187-195). Scope is the Erbium CPU subsystem as configured by config_build.vh: one neighbourhood of 8 minions plus L1 I$, CLINT/PLIC and the AXI bridge, with the shire cache stubbed and no memshire/mesh, s

##  | refuted=False | status->needs_tooling
CLAIM: 
REASON: Every cited source checks out on the erbium branch (HEAD b38a1a3 "Initial drop of the Erbium original RTL"). dv/standalone/cpu_subsystem/Makefile lines 16-24: `ifeq ($(VERILATOR),1) ... include $(REPOROOT)/dv/standalone/common/verilator.mk else ... include .../vcs.mk` (verified verbatim). dv/standalone/common contains only build_init_reg_config_file, common_cc.mk, common_msg.cfg, gen_defines.sh, vcs.mk; `git ls-tree -r HEAD` has no verilator.mk, and `git cat-file -e <c>:dv/standalone/common/verilator.mk` fails for every commit in the repo (both branches). dv/standalone/cpu_subsystem/rebuild lines 54-55 and 93-95 map `-verilator` to `export VERILATOR=1` and then run `make -f .../cpu_subsystem
CORRECTED: Same as stated, with corrected env.sh citations: dreams2perf prepend at env.sh lines 102-108, powerflow at 114-118, VERILATOR_HOME=/tools/verilator at 176-177, RUNSYN_HOME=$REPOROOT/tools/runsyn at 182-183; none of core-et/tools/{dreams2perf,powerflow,runsyn} exist in the erbium drop.

##  | refuted=False | status->needs_tooling
CLAIM: 
REASON: Every citation checks out verbatim: external/core-et/dv/standalone/cpu_subsystem/vsrc/top.sv lines 391-416 contain the +dump=%d / +traceEnable=%d plusarg block calling $dumpfile("trace.fst")+$dumpvars() under `ifdef VERILATOR and $vcdplusfile("trace.vpd")+$vcdpluson otherwise, with $dumpflush/$fflushall in a final block; csrc/Vtop_main.cpp lines 89-91 set contextp->traceEverOn(true) and threads(1); top.sv lines 187-195 (inside `ifdef TIMING, and `define TIMING is at line 4) toggle clock every #0.5ns (1 GHz) and clock_ext every #2.5ns; rtl/inc/timescale.vh gives 1ns/1ps under ET_SIMULATION; dv/tests/minion_core/regress_lists/minion.etdv.py lines 197-207 add '+fsdb+struct=on +traceEnable=1' an
CORRECTED: The standalone cpu_subsystem bench (external/core-et/dv/standalone/cpu_subsystem) contains source-level waveform hooks: `+dump=1` or `+traceEnable=1` runs $dumpfile("trace.fst")+$dumpvars() under `ifdef VERILATOR or $vcdplusfile("trace.vpd")+$vcdpluson under VCS (vsrc/top.sv:391-416), and csrc/Vtop_main.cpp:90 sets traceEverOn(true); the testbench clock is 1 GHz (top.sv:190, timescale 1ns/1ps), so one sim cycle = 1 ns. This is the literal per-cycle, per-bit mechanism, but only for the Erbium CPU

##  | refuted=False | status->needs_tooling
CLAIM: 
REASON: Every factual element of the claim checks out on the erbium drop (HEAD b38a1a3, branch erbium): dv/standalone/cpu_subsystem/Makefile line 20 does `include $(REPOROOT)/dv/standalone/common/verilator.mk`; dv/standalone/common contains only build_init_reg_config_file, common_cc.mk, common_msg.cfg, gen_defines.sh, vcs.mk; `git grep verilator.mk` over the erbium tree hits only that include line, and `git ls-tree -r` on refs/heads/erbium and refs/remotes/origin/erbium finds no verilator.mk. dv/standalone/cpu_subsystem/rebuild lines 54-55 map `-verilator` to `export VERILATOR=1`, and minion.etdv.py line 468 builds cpu_ss_vl via `rebuild -verilator`, so the README line 103 quick-start (`et-dvrun -t 
CORRECTED: On the erbium branch (b38a1a3) the cpu_subsystem Verilator build cannot run as-is: dv/standalone/cpu_subsystem/Makefile:20 includes dv/standalone/common/verilator.mk, which is absent from every erbium ref (only vcs.mk, common_cc.mk, gen_defines.sh, build_init_reg_config_file, common_msg.cfg ship), and et-dvrun's cpu_ss_vl build (minion.etdv.py:467-469) calls `rebuild -verilator` into the same include; its build check (line 460) even looks for the VCS artifact vbuild/simv. No simulator is install

##  | refuted=False | status->works_now
CLAIM: 
REASON: Every cited source says what the claim says, and the numbers reproduce, with one imprecision. Verified on external/core-et (branch erbium, sole commit b38a1a3 "Initial drop of the Erbium original RTL"): (1) README.md line 3 says the repo holds the "CORE-ET CPU Subsystem ... major AXI4 transaction initiator of the Erbium SoC"; line 9 "integrates one ET-Neighborhood"; the Repository Structure table at line 112 promises `memshire/` and `soctop/`, and `ls rtl/` shows only cpu_subsystem, inc, libs, shire. (2) rtl/cpu_subsystem/cpu_subsystem_top.v (586 lines) instantiates sys_gasket_reset (164), cpu_etl2axi_top (181), cpu_apb_mux (247), cpu_plic (268), rst_repeat (281), icache_data_ram_wrap (290),
CORRECTED: The open RTL is only the Erbium CPU subsystem: one neighbourhood of 8 minions plus L1 I-cache SRAM wrapper, PLIC, CLINT, FLB, FCC, an ET-Link-to-AXI bridge, and the reset gasket / APB mux glue. rtl/ has 4 subtrees: shire (353 files: minion 212, esr 80, neigh 50, shire_channel 3, uncached 2, scripts 6), libs (197), inc (84), cpu_subsystem (27); 433 .v/.sv files totalling 97,255 lines (115,774 with the 80 .vh headers) and 453 module definitions (418 unique names). The README's promised memshire/ a

##  | refuted=True | status->needs_tooling
CLAIM: 
REASON: The structural inventory of the erbium branch is essentially right (rtl/ = shire 353 / libs 197 / inc 84 / cpu_subsystem 27 files; 433 .v/.sv files; 453 `module` hits = 418 unique names; no memshire/ or soctop/ despite README.md:112; cpu_subsystem_top.v instantiates exactly etl2axi/apb_mux/plic/icache_data_ram/neigh_top/flb/fcc/esr_shire_other/clint at lines 164-538). But three things in the claim do not hold. (1) STATUS works_now is wrong: nothing on aifoundry2 can run this RTL — `which verilator` is empty, /tools/verilator and /tools/riscv-elf-toolchain (required by env.sh:176 and README) do not exist, and the erbium branch's own Verilator flow is broken at this commit: dv/standalone/cpu_s
CORRECTED: The public erbium branch of core-et contains one Erbium CPU subsystem (one ET-Neighbourhood = 8 dual-threaded minions = 16 harts, shire_defines.vh:24/35, plus shared 32 KB L1 I-cache wrapper, PLIC, CLINT, FLB, FCC, ESR block and an ET-Link-to-AXI bridge): rtl/ = shire 353 + libs 197 + inc 84 + cpu_subsystem 27 files; 433 .v/.sv files = 97,255 lines (115,774 including the 80 .vh headers); 453 module definitions (418 unique names). No memshire/, soctop/, shire-cache, mesh/NoC, PCIe, IO-shire, Maxi

##  | refuted=True | status->needs_tooling
CLAIM: 
REASON: The core mechanism is real and the RTL/checker citations check out, but the claim as stated is wrong on three points that matter for the report. (1) Golden model: dv/cosim/Makefile:39 defaults ERBIUM?=1 and line 73 adds -DERBIUM, i.e. the cosim links the *Erbium* configuration of sw-sysemu. In et-platform at 353f20e that configuration is a separate library/binary (sw-sysemu/CMakeLists.txt:421-462 builds `sw-erbium` with the ERBIUM define; lines 569-577 build `erbium_emu`), and it is installed as /opt/et/bin/erbium_emu, not /opt/et/bin/sys_emu (which is the ET-SoC-1 build). Same codebase, different chip config: the claim's 'the same functional simulator installed as sys_emu' conflates the two
CORRECTED: The Erbium RTL (core-et erbium branch) is co-simulated instruction-by-instruction against the Erbium configuration of sw-sysemu (the codebase behind /opt/et/bin/sys_emu, but built with -DERBIUM; the matching installed binary is /opt/et/bin/erbium_emu). arch_state_monitor.v (2,724 lines; 667 hierarchical references over 216 unique probe paths into intpipe/dcache/VPU/CSR) calls DPI inst_retired() with cycle, PC, instruction bits, integer/FP/mask writebacks, store address/data/mask, tensor and FLB 

##  | refuted=False | status->needs_tooling
CLAIM: 
REASON: Every cited location is accurate: dv/tools/dvutil.py:119-134 has the VCS `-cm line+cond+tgl+fsm+branch+assert ... -cm_tgl portsonly` (build) / `line+cond+tgl+fsm+branch+assert` (test) strings; dv/tests/minion_core/regress_lists/minion.etdv.py:150-153 (`ENABLE_COV=1`, `-cm line+tgl+branch+assert+fsm+cond -cm_dir test.vdb`), :368 (`+define+ET_PTPX_SIM=1`), :513-522 (fc/fc_ptpx/fc_ddr_ptpx builds calling `$REPOROOT/dv/standalone/fc/rebuild`), :869-871 (shire_cache ptpx); `ls dv/standalone` = common, cpu_clint, cpu_subsystem only (fc, shire_cache, minion_shire benches absent); origin/main mk/verilator.mk:39-44 has `--coverage` in COMMON_FLAGS and per-test `verilator_coverage --annotate`; origin/
CORRECTED: Per-signal toggle coverage is a shipped simulator option, but not a build option on this machine: no Verilator or VCS is installed (Verilator >= 5 required, origin/main README.md:66). The VCS flow (dv/tools/dvutil.py:119-134 `-cm ...tgl...`, minion.etdv.py:150-153) needs Esperanto-internal et-dvrun/slurm/license infrastructure and the fc/shire_cache/minion_shire benches that are absent from dv/standalone, and its default build variant is `-cm_tgl portsonly`; VCS tgl yields per-bit covered/uncove

##  | refuted=True | status->needs_tooling
CLAIM: 
REASON: The quoted citations are textually accurate (dvutil.py:129-134 default cm string incl. -cm_tgl portsonly; minion.etdv.py:152 '-cm line+tgl+branch+assert+fsm+cond -cm_dir test.vdb', :370 '+define+ET_PTPX_SIM=1', :513-522 fc/fc_ptpx builds, :869-871 shire_cache_ptpx; origin/main mk/verilator.mk:39-44 COMMON_FLAGS with --coverage and :61-64 verilator_coverage --annotate; origin/main Makefile:48-60 'for type in line branch toggle'; dv/standalone has only common/cpu_clint/cpu_subsystem; zero 'saif' hits in rtl/ dv/ mk/). But the headline 'a build option, not a research project' and the implied relevance to the lab card's bits are not supported: (1) No simulator is installed on this machine: 'veri
CORRECTED: Per-signal toggle coverage is switched on in the open DV flows: the erbium-branch VCS flow takes '-cm line+cond+tgl+fsm+branch+assert' (dvutil.py:129-134, default build restricted with -cm_tgl portsonly; minion.etdv.py:152 for full tgl), and the origin/main Verilator flow builds every hw/ip unit test with --coverage and reports line/branch/toggle via verilator_coverage (mk/verilator.mk:39-44,61-64; Makefile:48-60). It is still a tooling project here, not a build option: no VCS or Verilator is in

##  | refuted=True | status->needs_tooling
CLAIM: 
REASON: The evidence quotes check out (arch_state_monitor.v is 2,724 lines with 370 unique hierarchical probes; DPI prototype at lines 36-78; negedge block at 1074 calls inst_retired on MINION_CORE.wb_valid with cycle=soc_cycle; checker.cc 434-760/894 mismatch checks exist; dv_minion_top.vh is included at minion_top.v:642; extern/mtg and extern/riscv-tests-fork exist). But three parts of the claim are imprecise enough to mislead the report. (1) Cosim does not use the installed /opt/et/bin/sys_emu: it compiles sw-sysemu sources into libcosim.so via `include $(SYSEMU)/emu-er.mk` (external/core-et/dv/cosim/Makefile:135, ERBIUM?=1 at :39). emu-er.mk does not exist in et-platform at 353f20e (only emu.mk)
CORRECTED: The RTL can be co-simulated instruction-by-instruction against the sw-sysemu functional model (the same codebase as /opt/et/bin/sys_emu, but compiled from source into libcosim.so, and requiring a sw-sysemu tree at or after et-platform commit 8e7725d94 (2026-01-21) for emu-er.mk, i.e. newer than the card's 353f20e). arch_state_monitor.v (2,724 lines, 370 unique hierarchical probes into intpipe/dcache/csr_file/VPU) calls DPI inst_retired() on every writeback with soc cycle, minion/thread id, PC, i

##  | refuted=False | status->needs_tooling
CLAIM: 
REASON: Every cited location checks out at commit 353f20e and `git diff --stat 353f20e HEAD` over minion_debug.c, command_dispatcher.c, devicemanagement/, device-api/include/management-api/, device-management-application/, et-driver/ and DevicePcie.cpp is empty, so HEAD is identical. Verified: (1) command_dispatcher.c lines 187-190 route DM_CMD_MDI_SELECT_HART...DM_CMD_MDI_WRITE_MEM to minion_debug_request() with no #ifdef around it (the only #ifdef in that file is TEST_EVENT_GEN at 175-177), and line 651 unconditionally starts the MDI breakpoint-notify task. (2) minion_debug.c lines 728-807 switch on all 17 MDI msg_ids. (3) device_mgmt_api_spec.h lines 117-136 define codes 128-144 and 192. (4) Serv
CORRECTED: MDI is reachable from an ordinary user account today: the SP's BL2 (every build variant, including production) dispatches DM commands 128-144 to minion_debug.c; the host sends them via DeviceManagement::serviceRequest over /dev/etN_mgmt, which the driver registers as a misc device with mode 0666 (the udev rule's "et-soc1*" pattern does not match "etN_mgmt", so the misc-device mode is what makes it world-writable; no capability check in the ioctl path). The installed dev_mngt_service 1.22.0 lists

##  | refuted=True | status->works_now
CLAIM: 
REASON: Core mechanics check out, but the claim is imprecise in two ways that matter for the report. (1) Mis-scoped hart filter: CHECK_ALLOWED_HART_ID (minion_debug.c@353f20e lines 26-27; MDI_PRIVILEDGED_ACCESS is defined nowhere in the tree, so it is active) is applied only to the per-hart commands (breakpoint, single-step, GPR/CSR/mem, hart status: lines 390, 410, 436, 479, 514, 538, 558, 582, 611). mdi_select_hart (lines 285-306) and mdi_halt/resume (lines 335, 342) do NOT check shire_id or hart_id at all: a host caller can select shire 32 neighbourhoods 0-1 (harts 2048-2079, the MM runtime's threads, MM_RT_THREADS=0x0000FFFF at driver/minion_configuration.c:251) and the chip-wide HALT will halt 
CORRECTED: Selection and run control are coarse: SELECT_HART selects all 16 harts of every neighborhood touched by thread_mask (firmware passes ALL_MINIONS_MASK into HACTRL.hawindow and hart_mask), and HALT/RESUME write the chip-wide DMCTRL haltreq/resumereq in R_SP_MISC (0x5202_9008), which act on every selected hart across the chip. The minimum unit that can be halted from the host is one neighborhood (8 minions / 16 harts). Selection and halt/resume take any shire_id, including the master shire's firmwa

##  | refuted=True | status->needs_tooling
CLAIM: 
REASON: Core mechanism checks out; the scoping/safety part of the claim and one citation do not. VERIFIED: (a) mdi_enable/disable_single_step are stubs returning -1 with the SW-11247 comment at 353f20e (minion_debug.c lines 447-469) and unchanged at HEAD (diff 353f20e..HEAD on minion_debug.c and shared/minion_debug_interface is empty). (b) RTL intpipe_csr_file.v: line 1829 io_singleStep = dcsr.step & !debug; line 566 dcsr.cause = single_stepped ? 4 : ...; line 975 forces a debug interrupt after one retire; lines 1849-1851 latch single_stepped on io_retire. (c) MINION_CSR_DCSR_ADDRESS 0x7b0 at minion_csr.h:279. (d) mdi_write_csr (minion_debug.c:551-571) passes csr_name through unfiltered (only GDB in
CORRECTED: Single-stepping on silicon: the MDI ENABLE/DISABLE_SINGLE_STEP commands are stubs returning -1 ('Not supported in MDI lib', SW-11247) at commit 353f20e and at HEAD, but the Minion implements hardware single-step (dcsr.step -> one retire -> debug re-entry with dcsr.cause=4) and MDI WRITE_CSR passes any raw CSR address (e.g. dcsr 0x7b0) to a 'csrw' in the program buffer, so a host client (custom program against libdevicemanagement; the mgmt node is udev 0666, dev_mngt_service has no MDI argument h

##  | refuted=False | status->needs_tooling
CLAIM: 
REASON: The claim survives every code-path check at 353f20e. (1) Firmware: minion_debug.c is compiled into EVERY BL2 variant, production included (device-bootloaders/src/ServiceProcessorBL2/CMakeLists.txt:14 foreach over production/fast-boot/testframework/mdi_enabled/linux; :124 services/minion_debug.c; :151 links minion_debug_interface), minion_debug_init() is called unconditionally (common/main.c:440, outside the #if !(FAST_BOOT||TEST_FRAMEWORK) block that ends at :433), and command_dispatcher.c:187-190 routes DM_CMD_MDI_SELECT_HART..DM_CMD_MDI_WRITE_MEM to minion_debug_request (minion_debug.c:728-807). So it does not depend on the 'mdi_enabled' variant being flashed. (2) Host path: DeviceManageme
CORRECTED: MDI is reachable from an ordinary user account today (needs_tooling): BL2 at 353f20e compiles minion_debug.c into every variant including production and dispatches DM commands 128-144 to it; the host sends them with DeviceManagement::serviceRequest over /dev/etN_mgmt (mode 0666, verified on the lab host), and the runtime does not hold that node, so a client can run alongside kernels. dev_mngt_service refuses the MDI codes, but a small client can be linked against the installed /opt/et/lib/libDM.

##  | refuted=False | status->works_now
CLAIM: 
REASON: Every cited location says what the claim says, at commit 353f20e, and `git diff 353f20e HEAD` on minion_debug.c, the minion_debug_interface library, etsoc_memory.c and layout.h is empty (processor.cpp differs only by an `|| is_unavailable()` guard at enter_debug_mode, irrelevant). Verified: (1) csr_addr_lookup (xml2devaddr_map.h:15-65) has 72 initializers in a 112-entry array; counting gives index 32=MINION_CSR_DPC_OFFSET(0x7b1), 65/66/67=fflags/frm/fcsr (0x1/0x2/0x3), 68-71=sie/stvec/sscratch/sepc (0x104/0x105/0x140/0x141, minion_csr.h:848-890,1564); hpmcounter/cycle entries are commented out. mdi_read_csr (minion_debug.c:585) indexes the table with csr_name and no bounds check; mdi_write_c
CORRECTED: What a halted hart exposes today via the SP Minion Debug Interface (SP BL2 @353f20e, identical at HEAD): GPRs via READ_GPR (index<32) and DUMP_GPR (Read_All_GPR fills only x0..x30 of the 32 response slots); the PC via READ_CSR index 32 = dpc (0x7b1); and only seven other CSRs through READ_CSR (indices 65-67 fflags/frm/fcsr, 68-71 sie/stvec/sscratch/sepc), because csr_name is a gdb target.xml register index looked up in a 112-entry table that is otherwise zero and is not bounds-checked (indices <

##  | refuted=True | status->needs_tooling
CLAIM: 
REASON: The mechanics of the claim check out against et-platform 353f20e (and the same files are byte-identical at HEAD 836a4ab60): READ_CSR indexes the 112-entry csr_addr_lookup with no bounds check (minion_debug.c:585; xml2devaddr_map.h:15-65 gives exactly index 32=dpc, 65-67=fflags/frm/fcsr, 68-71=sie/stvec/sscratch/sepc), WRITE_CSR passes a raw CSR address (minion_debug.c:561-568), READ_MEM's whitelist and the hart-executed vs SP-memcpy split are as described (minion_debug.c:129-154,616-672; MDI_PRIVILEDGED_ACCESS is not defined so only the 4 unprivileged regions apply), Read_All_GPR loops 0..30 (minion_state_inspection.c:33), and the RTL confirms debug mode runs at M privilege with CSR privileg
CORRECTED: What a halted hart can expose via the SP Minion Debug Interface (firmware at 353f20e, identical at HEAD): GPRs one at a time through READ_GPR (DUMP_GPR is broken: it pushes header+8 B while advertising header+256 B, desynchronising the SP completion queue); the PC via csr_name index 32 -> dpc; and only fflags/frm/fcsr (65-67) and sie/stvec/sscratch/sepc (68-71) through READ_CSR, whose csr_name indexes an unchecked 112-entry table that is otherwise zero (other indexes silently read CSR 0, the res

##  | refuted=True | status->works_now
CLAIM: 
REASON: The register-level mechanism checks out at commit 353f20e (identical to HEAD: `git diff 353f20e HEAD` on the three debug files is empty): DMCTRL = R_SP_MISC_BASEADDR 0x0052029000 (etsoc-hal/include/hwinc/hal_device.h:143) + SPIO_MISC_ESR_DMCTRL 0x8 (hwinc/sp_misc.h:25-26) = 0x5202_9008, matching PRM 12473-12475; HACTRL/HASTATUS0/HASTATUS1 are REGION_NEIGHBOURHOOD ESRs (debug_accessor.h:91-164) and core-et esr.md:3255-3336 documents them as per-neighborhood 16-hart masks; ABSCMD index 0xf8 / NXDATA0 0xf0 (debug_accessor.h:44-49) match RTL minion_types.vh:232,240 and esr.md:741-749 (0x780/0x7C0 byte offsets); execute_instructions() writes 2-instruction pairs (debug_accessor.c:121-155) each ter
CORRECTED: The minion debug path is an Esperanto-specific, memory-mapped debug module: DMCTRL (dmactive/haltreq/resumereq, broadcast to all Minion shires) is a 32-bit register at SPIO-MISC 0x5202_9008 in the service processor's address space; hart selection and status are per-neighborhood 16-hart-mask ESRs HACTRL/HASTATUS0/HASTATUS1 (PP=debug, SP privilege); a halted hart executes 2-instruction, ebreak-terminated pairs written to its per-hart ABSCMD ESR (index 0xf8) and returns data through the ddata0 CSR 

##  | refuted=True | status->needs_tooling
CLAIM: 
REASON: The mechanism description (neighborhood-granular SELECT, chip-wide DMCTRL HALT/RESUME) is confirmed, but two parts of the claim fail: (1) STATUS works_now is wrong for the lab card. The only installed sender of DM commands, /opt/et/bin/dev_mngt_service, lists codes 128-144 (DM_CMD_MDI_*) in --help and validCode() accepts them (dev_mngt_service.cc 1509-1525 @353f20e, table from DeviceManagement.h 101-117), but verifyService() has no MDI case at all (grep -i mdi on the file returns nothing) so every MDI code hits the default at line 1463-1465: 'Aborting, command: ... is currently unsupported', return -EINVAL. No other installed binary sends MDI commands (et-powertop only embeds the name table)
CORRECTED: Selection and run control are coarse in the flashed firmware: SELECT_HART selects all 16 harts of every neighborhood touched by the 64-bit per-shire thread_mask (Select_Harts always passes ALL_MINIONS_MASK into both hawindow and hartmask), and HALT/RESUME write the chip-wide, SP-private DMCTRL haltreq/resumereq, which act on every selected hart across the chip. Minimum halt unit is one neighborhood (8 minions = 16 harts); hardware (HACTRL.hartmask, neigh_ch_dbg.v:44,156) is per-hart, so single-h

##  | refuted=False | status->works_now
CLAIM: 
REASON: Every cited location says what the claim says at commit 353f20e, and none of the cited files differ between 353f20e and HEAD (git diff --stat is empty for minion_debug.c, minion_configuration.c, minion_debug_interface/, kw.c, et-soc1-pcie.c, command_dispatcher.c). Verified: minion_debug.c:346-355 mdi_reset_hart() only logs and replies (comment at line 352 'MDI lib does not support reset hart functionality yet'); kw.c:105-115 KW_REGISTER_CM_ABORT_TIMER with the quoted warning, KERNEL_CM_ABORT_WAIT_TIMEOUT=5 (kw.h:46), and the timeout callback (kw.c:229-236) only sets a flag and IPIs the KW, it never halts or resets the CM hart; et-soc1-pcie.c:1137-1153 mgmt open returns -EBUSY when already op
CORRECTED: NOTES correction: 'The SP has no watchdog on the master minion' is wrong as written; the SP creates a 10 s MM heartbeat watchdog (main.c:518 -> MM_Init_HeartBeat_Watchdog, minion_configuration.c:1892-1907; MM_HEARTBEAT_TIMEOUT_MSEC=10000, minion_configuration.h:43), started on the first MM heartbeat (minion_configuration.c:1662-1674). But on expiry MM_HeartBeat_Timer_Cb (547-560) only calls Minion_State_MM_Error_Handler(SP_RECOVERABLE_FW_MM_HANG), which increments a hang counter and, past the th

##  | refuted=True | status->needs_tooling
CLAIM: 
REASON: The mechanism description checks out (every cited register, macro, PRM line and errata verified; one line-number slip: apb_pslverr is minion_debug_apb_slv.v:170, not 149-153; errata bodies are numbered 2.19 at lines 826-865 and 2.27 at 1155-1192, the TOC calls them 1.19/1.27). But the ACCESS/STATUS/GRANULARITY fields are wrong for the practical lens. (1) ACCESS is not "service processor only": the BL2 flashed from 353f20e (every build variant, production included, compiles services/minion_debug.c and rtos_task/mdi_debug_task.c: ServiceProcessorBL2/CMakeLists.txt:105,124,151) dispatches 17 host-facing commands DM_CMD_MDI_SELECT_HART..DM_CMD_MDI_WRITE_MEM (ids 128-144, device_mgmt_api_spec.h:1
CORRECTED: The minion debug path is an Esperanto-specific ESR-mapped debug module (DMCTRL at SPIO-MISC 0x5202_9008, per-neighborhood HACTRL/HASTATUS0/1, per-hart ABSCMD 2-instruction pairs, results via ddata0 CSR 0x7b8 mirrored as NXDATA0/1). It is driven by the service processor, and the SP exposes it to the HOST as 17 DM commands (DM_CMD_MDI_* 128-144: select/unselect/halt/resume hart, hart status, set/unset PC breakpoint with async event 192, read/dump/write GPR, read/write CSR, read/write memory) that 

##  | refuted=False | status->needs_tooling
CLAIM: 
REASON: Every element of the claim checks out against the erbium drop (HEAD b38a1a3 == erbium branch, no diff). dv/standalone/cpu_subsystem/Makefile lines 16-20 do `include $(REPOROOT)/dv/standalone/common/verilator.mk` when VERILATOR=1; dv/standalone/common contains only build_init_reg_config_file, common_cc.mk, common_msg.cfg, gen_defines.sh, vcs.mk, and `git ls-tree -r HEAD` lists no verilator.mk anywhere (not gitignored either). The rebuild script (dv/standalone/cpu_subsystem/rebuild) maps `-verilator` to `export VERILATOR=1` and then runs `make -f .../Makefile all`, so the cpu_ss_vl build in minion.etdv.py:467-469 hits the missing include; GNU make aborts on a missing non-optional include. Veri
CORRECTED: 

##  | refuted=True | status->needs_tooling
CLAIM: 
REASON: The RTL inventory in the claim is essentially right, but two load-bearing parts are not supported by the sources: (1) STATUS works_now is wrong for the "offline_simulation" access it describes. On this machine neither verilator nor vcs is installed (`which verilator vcs` empty; /tools, the README-prescribed install prefix, does not exist; /opt/et/bin has no simulator). Worse, the shipped standalone flow is incomplete: dv/standalone/cpu_subsystem/Makefile:9 defaults to VERILATOR?=0 (Synopsys VCS, commercial) and with VERILATOR=1 it `include`s $(REPOROOT)/dv/standalone/common/verilator.mk (Makefile:20), a file that does not exist anywhere in the repo (git ls-files dv/standalone/common lists on
CORRECTED: The open RTL (core-et erbium branch, single commit b38a1a3) is the Erbium CPU subsystem: one ET-Neighbourhood of 8 dual-threaded minions (rtl/inc/shire_defines.vh:24,35) plus L1 I-cache SRAM wrapper, PLIC, CLINT, FLB, FCC, subsystem ESRs, APB mux, reset gaskets and an ET-Link-to-AXI bridge (rtl/cpu_subsystem/cpu_subsystem_top.v:164-538). rtl/ has four subtrees (shire 353 files, libs 197, inc 84, cpu_subsystem 27); the .v/.sv sources total 97,255 lines in 433 files (115,774 lines if the 80 .vh he

##  | refuted=False | status->needs_tooling
CLAIM: 
REASON: Every cited location says what the claim says. (1) /home/yaroslavvb/claude/et-soc1-prototyping/external/core-et/dv/standalone/cpu_subsystem/Makefile lines 16-24: `ifeq ($(VERILATOR),1) ... include $(REPOROOT)/dv/standalone/common/verilator.mk else ... include $(REPOROOT)/dv/standalone/common/vcs.mk`. (2) dv/standalone/common/ contains only build_init_reg_config_file, common_cc.mk, common_msg.cfg, gen_defines.sh, vcs.mk; `git ls-tree -r HEAD` lists no verilator.mk anywhere; the only reference to the string verilator.mk in the tree is that Makefile line 20. GNU make treats a plain `include` of a missing file as fatal, so `make ... all` dies before any compile. (3) dv/standalone/cpu_subsystem/r
CORRECTED: On the erbium branch (single commit b38a1a3, 'Initial drop of the Erbium original RTL') the cpu_subsystem Verilator build cannot run as-is: Makefile line 20 includes dv/standalone/common/verilator.mk, which is absent from the drop (only vcs.mk, common_cc.mk, gen_defines.sh, build_init_reg_config_file, common_msg.cfg ship), so `rebuild -verilator` (which sets VERILATOR=1 and invokes that Makefile) and the etdv 'cpu_ss_vl' build both fail at make's include step; the README quick-start additionally

##  | refuted=False | status->needs_tooling
CLAIM: 
REASON: The mechanism survives scrutiny but the claim is imprecise on granularity and on what the host can see. Confirmed at et-platform 353f20e: (1) DM_CMD_MDI_ENABLE/DISABLE_SINGLE_STEP are stubs returning -1 (ServiceProcessorBL2/services/minion_debug.c lines 447-469, 'Not supported in MDI lib', SW-11247); `git diff 353f20e HEAD` on minion_debug.c and shared/minion_debug_interface is empty, so HEAD is identical. (2) mdi_write_csr (minion_debug.c ~549-569) passes csr_name through unchanged except GDB index 32 -> DPC, and Write_CSR (minion_state_inspection.c 47-52) executes WRITE_CSR_SEQ = `csrw <offset>, x31` (debug_instruction_sequence.h 51-57, opcode 0xf9073 | offset<<20) from the abstract-comman
CORRECTED: Single-stepping on silicon: the MDI ENABLE/DISABLE_SINGLE_STEP commands are stubs returning -1 (SW-11247) at 353f20e and at HEAD, but the Minion implements hardware single-step (dcsr.step -> flush pipe, re-enter debug with cause 4) and MDI WRITE_CSR forwards the raw CSR address (only GDB index 32 is remapped to dpc), executing `csrw 0x7b0, x31` from the abstract-command buffer. A user-mode client linking /opt/et/lib/libDM.so (mgmt node is 0666, exclusive-open, and not held by runtime sessions) c

##  | refuted=True | status->needs_tooling
CLAIM: 
REASON: The four citations are accurate as quotes, but the claim as stated ("bit-level waveform capture EXISTS in the standalone bench ... every net/flop/SRAM bit in cpu_subsystem_top") overstates what the erbium branch delivers on three counts. (1) Only the $dumpvars CALL exists; the Verilator build flow does not. dv/standalone/cpu_subsystem/Makefile line 20 does `include $(REPOROOT)/dv/standalone/common/verilator.mk`, but dv/standalone/common/ contains only build_init_reg_config_file, common_cc.mk, common_msg.cfg, gen_defines.sh, vcs.mk (verified by ls; `git log --all -- '*verilator.mk'` finds it only in origin/main's mk/verilator.mk, a different tree). `./rebuild -verilator` (rebuild lines 55-56)
CORRECTED: The erbium-branch standalone bench (dv/standalone/cpu_subsystem/vsrc/top.sv lines 393-409) contains a waveform hook: `+dump=1` or `+traceEnable=1` calls $dumpfile("trace.fst")+$dumpvars() under `ifdef VERILATOR, else $vcdplusfile("trace.vpd")+$vcdpluson, and csrc/Vtop_main.cpp line 89 sets traceEverOn(true). It is not runnable as shipped: the Makefile (line 20) includes dv/standalone/common/verilator.mk, which is absent from the branch, verilator is not installed here, and the VCS path (common/v

##  | refuted=True | status->needs_tooling
CLAIM: 
REASON: The erbium-branch inventory is mostly right, but the claim's scope word "only", its line/file figures, its -DERBIUM note and its works_now status do not survive checking.

1. SCOPE ("the open RTL is only the Erbium CPU subsystem") is wrong at repository level. The same clone carries origin/main (`git -C external/core-et branch -a` shows erbium + main; checked out HEAD is b38a1a3 detached, single commit). origin/main has hw/ip/ with 464 .v/.sv files and 114,685 lines (`git ls-tree -r origin/main`), including a clean-room SystemVerilog re-implementation of the ET-SoC-1 shire cache: hw/ip/shirecache (93 files: shirecache_top/xbar/bank/pipe/sub_bank, bank_perfmon + perfmon_ctl + perfmon_qual, ba
CORRECTED: Open RTL for the ET-SoC-1 comes in two layers of one repository. (a) erbium branch (checked out, commit b38a1a3): the original Erbium CPU-subsystem RTL — one 8-minion/16-hart neighbourhood (rtl/shire: minion, neigh, esr, shire_channel/icache, uncached FLB/FCC) plus PLIC, CLINT, ET-Link-to-AXI bridge and the shared 32 KB L1 I-cache wrapper (rtl/cpu_subsystem); 433 .v/.sv files = 97,255 lines (115,774 with the 80 .vh headers), 453 modules; README's memshire/ and soctop/ do not exist. (b) main bran

##  | refuted=True | status->needs_tooling
CLAIM: 
REASON: The claim's individual evidence lines check out, but its headline ("a build option, not a research project") and the granularity/access story do not survive the practical-usability lens. Verified true: dv/tools/dvutil.py lines 126-134 emit `-cm line+cond+tgl+fsm+branch+assert ... -cm_tgl portsonly` (build) / `-cm line+cond+tgl+fsm+branch+assert` (test) only when `--uarg CM=default` is passed; dv/tests/minion_core/regress_lists/minion.etdv.py lines 150-153 add `-cm line+tgl+branch+assert+fsm+cond -cm_dir test.vdb` only with the `coverage` user arg; lines 513-522 and 869-871 build `fc_ptpx`/`shire_cache_ptpx` from `$REPOROOT/dv/standalone/fc/rebuild` and `.../shire_cache/rebuild`, and both dir
CORRECTED: Per-signal toggle counting is supported by the shipped DV scripts only as VCS `-cm ... tgl` options (dv/tools/dvutil.py:126-134, minion.etdv.py:150-153, opt-in via `--uarg CM=default`/`coverage`), and by Verilator `--coverage` in the origin/main ETASP per-IP unit tests and rtlcosim (mk/verilator.mk:39-44, Makefile coverage-report line/branch/toggle). On aifoundry2 neither VCS nor Verilator is installed, the erbium drop's Verilator include (dv/standalone/common/verilator.mk) is missing so only th

##  | refuted=True | status->needs_tooling
CLAIM: 
REASON: The quoted source lines are accurate (top.sv 391-416 has the +dump=/+traceEnable= -> $dumpfile("trace.fst")/$dumpvars() `ifdef VERILATOR / $vcdplusfile `else block; Vtop_main.cpp:89 traceEverOn(true); TIMING is `define'd at top.sv:4 so the 1 GHz `#0.5ns` clock is live), and needs_tooling is the right bucket, but the claim overstates what "exists": (1) The Verilator build path is dead as checked out: dv/standalone/cpu_subsystem/Makefile:20 does `include $(REPOROOT)/dv/standalone/common/verilator.mk`, and that file exists in no commit on any branch (dv/standalone/common/ holds only build_init_reg_config_file, common_cc.mk, common_msg.cfg, gen_defines.sh, vcs.mk; `git log --all -- dv/standalone
CORRECTED: Bit-level waveform dumping is written into the erbium-branch standalone bench source (dv/standalone/cpu_subsystem/vsrc/top.sv:391-416: +dump=1/+traceEnable=1 -> $dumpfile("trace.fst")+$dumpvars() under VERILATOR, $vcdplusfile("trace.vpd")/$vcdpluson under VCS; csrc/Vtop_main.cpp:89 traceEverOn(true); 1 GHz `#0.5ns` clock enabled by `define TIMING at top.sv:4), but it is not buildable as checked out: Makefile:20 includes dv/standalone/common/verilator.mk, which exists in no commit; no --trace-fst

##  | refuted=False | status->needs_tooling
CLAIM: 
REASON: Every cited location says what the claim says. dv/tools/dvutil.py lines 128-131 (erbium, HEAD b38a1a3) contain the exact CM=default strings including `-cm_tgl portsonly` for build and `line+cond+tgl+fsm+branch+assert` for test; minion.etdv.py lines 152, 370, 513-522, 869-871 match verbatim; dv/standalone contains only common, cpu_clint, cpu_subsystem (no fc/shire_cache/memshire/dft benches); origin/main mk/verilator.mk lines 39-44 has `--coverage` in COMMON_FLAGS and lines 61-64 run verilator_coverage --annotate; origin/main Makefile:48-60 loops `for type in line branch toggle` with --filter-type/--write-info; a repo-wide case-insensitive grep for saif/primetime/pt_shell/liberty finds nothin
CORRECTED: Per-signal toggle observation in RTL simulation is a build option rather than a research project, with caveats. The erbium-branch VCS flow (dv/tools/dvutil.py:128-131, minion.etdv.py:152) enables `-cm line+cond+tgl+fsm+branch+assert` coverage, but the CM=default build limits toggle coverage to ports (`-cm_tgl portsonly`; per-internal-signal needs a custom `--uarg CM=` string), VCS toggle coverage is hit/miss per bit per direction (not a transition count), and VCS is a licensed commercial tool no

##  | refuted=False | status->needs_tooling
CLAIM: 
REASON: Core mechanism verified and accurate, but two framing details are imprecise and the 'needs_tooling' gap is larger than the claim implies. VERIFIED: (1) dv/arch_monitors/vsrc/arch_state_monitor.v is 2724 lines; the DPI prototype at lines 36-78 matches the quote exactly (cycle, minion_id, thread_id, pc, inst_bits, wbx/wbf/wbm writebacks, store addr/size/data/mask, scatter, tensor_load/fma/quant/store, flb_valid/flb_dest/flb_match_idx, prv); the call is at line 1123, inst_exception at 1172; `cycle` is `soc_cycle` (line 175), so the cycle stamp is the SoC-level counter. (2) checker.cc (1323 lines) emu_inst() at 290-336 steps the emulator in lockstep per retired RTL instruction (check_pending_int
CORRECTED: The Erbium RTL is co-simulated instruction-by-instruction against the sw-sysemu functional model compiled from source into libcosim.so (dv/cosim/Makefile 135-142), not against the /opt/et/bin/sys_emu binary; the sysemu version it needs is et-platform HEAD (requires sw-sysemu/emu-er.mk added 2026-01-21 and SYS_EMU-guarded cosim-mode fixes from Jan-Mar 2026), which post-dates the lab card's 353f20e build. arch_state_monitor.v (2724 lines) calls DPI inst_retired() per retired instruction per hart w

##  | refuted=True | status->needs_tooling
CLAIM: 
REASON: The hazard facts cited are real (reset-hart stub, KW abort timer, single-open mgmt node, debug clock gate write, errata 2.18, no MM->SP watchdog), but the claim is wrong or overstated on the points that matter for practical use. (1) status=works_now is wrong: /dev/et0_mgmt is 0666 and the driver has no capable() check, but the only installed client, /opt/et/bin/dev_mngt_service, lists DM_CMD_MDI_* in --help and then rejects every one of them in verifyService() (no MDI case; default: "is currently unsupported", -EINVAL) before touching the device; the 2022 dm_tester GDB server is gone at 353f20e and no MDI test binary is installed, so a custom libDM.so client must be written -> needs_tooling.
CORRECTED: MDI on the shared lab card is reachable by a non-root user only in principle: /dev/et0_mgmt is 0666 and the driver has no capability check, but no installed tool can issue MDI commands (dev_mngt_service rejects all DM_CMD_MDI_* as "currently unsupported"; the old dm_tester GDB server is gone), so a client must be written against /opt/et/lib/libDM.so. MDI halt/resume granularity is one neighbourhood (16 harts) per SELECT and chip-wide per HALT/RESUME; per-hart granularity exists only for GPR/CSR/

##  | refuted=True | status->needs_tooling
CLAIM: 
REASON: Most of the claim's mechanics check out, but two load-bearing statements are wrong or misleading. (1) "The full 1088-minion chip was never RTL-simulated at scale" is contradicted by the regress list: external/core-et/dv/tests/minion_core/regress_lists/minion.etdv.py:507-511 defines a VCS build 'fc' with +define+DV_MINION_SHIRE_ALL_NEIGH_STUB=0xe00000000 (36-bit mask, only bits 33-35 stubbed, i.e. all 33 compute minion shires with real neighborhoods), lines 769-772 define 'fc_full' ("33 Minion Shires with IO Shire, 2 MEM Shires and P Shire, real DDR"), 757-761 'fc_all', 776-782 'fc_gls' (gate-level full chip), and minion_diags.py:477 runs test fc_smoke on build 'fc' with +SHIRE_MASK=0x1ffffff
CORRECTED: No measured RTL simulation rate is stored anywhere in the drop (evl_verif_comp_map.sv:1426-1441 only prints cycles/second at run time). The shipped Verilator standalone bench (dv/standalone/cpu_subsystem) uses Verilator --timing semantics (`define TIMING, top.sv:4; `forever #0.5ns` clocks, top.sv:188-195) and a runtime context of threads(1) (Vtop_main.cpp:91), but its build is incomplete: the Makefile includes dv/standalone/common/verilator.mk (Makefile:19-21), which is absent from the drop, so 

##  | refuted=True | status->needs_tooling
CLAIM: 
REASON: Every quoted excerpt is accurate at the cited line (Vtop_main.cpp 39-57/59-83/91; evl_verif_comp_map.sv:1439; zebu_2112t.ini:6-9; minion.etdv.py:1513-1515; sys_emu -max_cycles), and 'no measured simulation rate in the drop' holds (only a runtime print at evl_verif_comp_map.sv:1432-1441 and a metrics-DB uploader dv/tools/etdv/lib/ETdv/dvrun/metrix.py; no logs). But the claim as stated is wrong or unsupported in four places. (1) 'The full 1088-minion chip was never RTL-simulated at scale' is contradicted by the regress list: dv/tests/minion_core/regress_lists/minion.etdv.py:508-511 defines a VCS full-chip build `fc` with `+define+DV_MINION_SHIRE_ALL_NEIGH_STUB=0xe00000000` (per dv/standalone/c
CORRECTED: No measured RTL simulation rate exists in the drop (only a runtime cycles/second print at dv/common/base/base/evl_verif_comp_map.sv:1432-1441). The only Verilator bench shipped, dv/standalone/cpu_subsystem, models the Erbium CPU subsystem: one 8-minion neighborhood (rtl/cpu_subsystem/cpu_subsystem_top.v:339 `neigh_top i_neigh`) with 8-lane VPUs (minion_defines.vh:189), CLINT/PLIC/etl2axi, no shire cache, NoC or memshire; it is single-context (`contextp->threads(1)`, Vtop_main.cpp:91), uses a tim

##  | refuted=False | status->needs_tooling
CLAIM: 
REASON: Every cited location says what the claim says: arch_state_monitor.v:34-78 is the inst_retired DPI prototype with cycle/minion_id/thread_id/pc/inst_bits/wbx/wbf/wbm/store/scatter/tensor/flb/prv args; :1074-1078 is `always_ff @(negedge clock)` gated on `MINION_CORE.wb_valid` and :1123 the call; checker.cc:434-894 emits the PC/Inst/Int/FP/Mask/Memory/MSTATUS/MCAUSE/TVAL/EPC/TensorFMA-mask mismatch strings and steps the emulator one instruction per event (checker.cc:379-425, `take_trap`/`advance_pc`); cosim.cc:24-28 includes system.h/emu_gio.h/esrs.h/processor.h/traps.h from $(SYSEMU); Makefile:5-7,39,72-74,92 as quoted; dv_minion_top.vh:6-36 instantiates arch_state_monitor under ENABLE_BEMU and
CORRECTED: The Erbium RTL (core-et erbium branch: one 8-minion neighbourhood, the same Minion core lineage as ET-SoC-1 but with known deviations such as evict-to-L2 clamping and no L2/memshire/SP) is co-simulated instruction-by-instruction against sw-sysemu built in its Erbium configuration (/opt/et/bin/erbium_emu; same source as sys_emu, different topology/PA-size build). arch_state_monitor.v (2,724 lines, ~370 hierarchical probes) calls DPI inst_retired() on every wb_valid with cycle, PC, instruction bit

##  | refuted=False | status->works_now
CLAIM: 
REASON: Every cited location says what the claim says. intpipe_csr_file.v:853 builds events = ZX(32,{io_events_ext, instret_up[1:0], cycle_up, 1'b0}); io_events_ext = {vpu[10:0], dcache[11:0], branch[1:0]} via intpipe_top.v:701-702 and core_top.v:589; widths from csr_defines.vh:42-46 (32/12/11/2) and macros.vh:24 (ZX zero-extends, so ev29-31 are 0 = PRM 'Reserved'). Walking the concatenation LSB-first gives ev1=CYCLES, ev2/3=instret t0/t1, ev4/5=branch t0/t1 ({wb_thread_id,!wb_thread_id} puts t0 in the low bit), ev6-17=dcache[0..11] in dcache_top.v:4289-4300 order, ev18-28=vpu[0..10] per vpu_defines.vh:236-246 as wired in vpu_ctrl.v:1681-1760. This matches PRM Table 1-3 (PRM.txt:393-440), pmu.h:130-
CORRECTED: As stated, with two small edits: the errata are 2.12 (Errata.txt:541-568, RTLMIN-6221) and 2.22 (Errata.txt:940-980, RTLMIN-6488) in the text export; and the per-counter increment is additionally gated by the neighbourhood pmu_ctrl clock-enable (neigh_pmu.v:68-69,141) and by the registered |event_sel enable (csr_file.v:880-884), not only by !reg_debug.

##  | refuted=True | status->needs_fw_change
CLAIM: 
REASON: The hardware part of the claim is confirmed bit-exact, but the STATUS/ACCESS pair is wrong under the practical lens, and the granularity is not quite what a reader gets. (1) RTL encoding CONFIRMED: intpipe_csr_file.v:853 `events = ZX(32,{io_events_ext, instret_up, cycle_up, 1'b0})` with instret_up[1:0] (csr_file.v:836) and io_events_ext = {io_events_vpu[10:0], io_events_dcache[11:0], branch_taken[1:0]} (intpipe_top.v:701-702, core_top.v:589); dcache bits 0-11 at dcache_top.v:4289-4306 and VPU bits 0-10 at vpu_defines.vh:236-246 / vpu_ctrl.v:1680-1699 line up 1:1 with PRM Table 1-3 (PRM:397-437) and pmu.h:130-158 at 353f20e (pmu.h identical at HEAD, empty diff). Increment gating `hpm_up[i][t]
CORRECTED: The minion PMU event encodings in PRM Table 1-3 and firmware pmu.h are bit-exact against the Erbium RTL (event vector {vpu[10:0], dcache[11:0], branch_taken[1:0], instret[1:0], cycle, 0}; counters gated by !debug and by neighbourhood pmu_ctrl.disable_clock; the neighbourhood adder sums the 8 minions' selections so a minion counter can advance up to +8/cycle). However, event selection is M-mode only and the firmware at 353f20e (and HEAD) exposes no syscall or launch option to change it (SYSCALL_S

##  | refuted=True | status->works_now
CLAIM: 
REASON: The syscall-dispatch part of the claim is fully confirmed at 353f20e (WorkerMinion/src/syscall.c:29-78 forwards exactly 1-11, 301, 302; default returns SYSCALL_INVALID_ID; MachineMinion/src/syscall.c:130-141 maps 19->sample_sc_pmcs(arg1,arg2,arg3), 20->sample_ms_pmcs(arg1,arg2), 23/24 SAMPLE_ALL whose only callers are MasterMinion/src/workers/statw.c:212,221,341,357; medeleg=0x5C00B1F7 at MachineMinion/src/main.c:130 delegates U-mode ecall (bit 8) but not S-mode ecall (bit 9), matching the U->S->M chain; pmu.c:48-81 stop/sample/start with ms_id<8 check only; `git diff --stat 353f20e HEAD` on the cited files is empty, only 3 python scripts differ in device-minion-runtime). The clause 'No path
CORRECTED: At firmware 353f20e the worker (S-mode) syscall handler (device-minion-runtime/src/WorkerMinion/src/syscall.c:29-78), entered only on EXCEPTION_ENVIRONMENT_CALL_FROM_U_MODE (trap_handler.S:80-85; medeleg 0x5C00B1F7 delegates cause 8 but not 9), forwards exactly U-mode numbers 1 EVICT_SW, 2 FLUSH_SW, 3 LOCK_SW, 4 UNLOCK_SW, 5 INVALIDATE, 6 EVICT_L1, 7 SHIRE_CACHE_BANK_OP, 8 RETURN_FROM_KERNEL (handled in S-mode), 9 PMC_SC_SAMPLE, 10 PMC_MS_SAMPLE, 11 EVICT_WHOLE_L1_L2, 301 CACHE_CONTROL, 302 FLUS

##  | refuted=False | status->works_now
CLAIM: 
REASON: Core claim verified line-by-line at 353f20e: WorkerMinion/src/syscall.c:29-78 forwards exactly numbers 1-7, 11, 301, 302, 9, 10 (8 handled locally via return_from_kernel), default -> SYSCALL_INVALID_ID; syscall.h:9-12 marks 301/302/309/310 as 'Legacy' and 309/310 are unhandled anywhere; MachineMinion/src/syscall.c:130-141 maps 19->sample_sc_pmcs, 20->sample_ms_pmcs, 23/24->*_all, and 23/24 are called only from MasterMinion/src/workers/statw.c (S-mode). medeleg=0x5C00B1F7 (MachineMinion/src/main.c:129-130, bit 8 set) delegates U-mode ecall to S-mode and WorkerMinion/src/trap_handler.S:80-84 dispatches every ECALL_FROM_U to syscall_handler with no range filter, so the path is live. pmu.c:48-81
CORRECTED: At firmware 353f20e the worker (S-mode) syscall handler forwards exactly U-mode numbers 1 EVICT_SW, 2 FLUSH_SW, 3 LOCK_SW, 4 UNLOCK_SW, 5 INVALIDATE, 6 EVICT_L1, 7 SHIRE_CACHE_BANK_OP, 11 EVICT_WHOLE_L1_L2, 301 CACHE_CONTROL, 302 FLUSH_L3, 9 PMC_SC_SAMPLE, 10 PMC_MS_SAMPLE to M-mode and handles 8 RETURN_FROM_KERNEL locally; anything else returns SYSCALL_INVALID_ID (-1). 309/310 are legacy defines, unhandled. M-mode maps 19->sample_sc_pmcs(shire, bank, pmc), 20->sample_ms_pmcs(ms, pmc); 23/24 SAM

##  | refuted=False | status->needs_fw_change
CLAIM: 
REASON: Every cited location says what the claim says. (1) external/core-et/rtl/inc/memshire_defines.vh:215-250 defines exactly the five qualifier formats with the stated bit positions (MCOP bank 27:20, ECC 19:17, mc1 16, mc0 15, cmd 14:0; MCDATA mc1/mc0/wr/rd = 3/2/1/0; MCMISC wrcomb 7, ieblk 6, war 5, raw 4, waw 3, rdwr 2, mc1 1, mc0 0; MESHCNT addr_base 63:38, addr_mask_size 37:33, addr_en 32, src_exact 28:20, src_exact_en 31, minshire 8, pshire 7, ioshire 6, qoshi 5, qoslo 4, mc1 3, mc0 2, wr 1, rd 0; MESHOUT wr 1 rd 0; QUAL2 min 11:0 max 27:16; MODE op 7:6 type 5:0). (2) external/core-et/dv/soc/memshire/evl_memshire_perfmon_itf.sv:42-77 lists per-controller u0/u1_umctl2_perf_op_is_{rd,wr,mwr,ac
CORRECTED: The memshire performance monitor's five qualifier formats are defined in core-et (rtl/inc/memshire_defines.vh:215-250): MCOP = cmd[14:0] with mc0 (15) / mc1 (16) selects, ECC 19:17, bank mask 27:20; MCDATA = rd(0)/wr(1) with mc0(2)/mc1(3); MCMISC = mc0(0)/mc1(1)/rdwr(2)/waw(3)/raw(4)/war(5)/ieblk(6)/wrcomb(7); MESHCNT = rd(0)/wr(1)/mc0(2)/mc1(3)/qoslo(4)/qoshi(5)/ioshire(6)/pshire(7)/minshire(8), exact 9-bit AXI source 28:20 with enable 31, address filter (enable 32, mask size 37:33, base 63:38)

##  | refuted=False | status->needs_fw_change
CLAIM: 
REASON: Every cited location says what the claim says, and I found no HEAD-vs-353f20e drift and no Erbium-vs-A0 discrepancy for this claim. Verified: (1) external/core-et/rtl/inc/shire_cache_defines.vh:471-490 has exactly the EV_RBUF/MSG/TAG=0/1/2 and the two RSRC 0-4 sets. (2) SC spec §4.9 (scratchpad scspec.txt, pdftotext of external/core-et/docs/CORE-ET-Shire-Cache-Specification.pdf v1.1): lines 4356-4360 one six-ESR group per bank, three 40-bit counters; 4370-4373 'if the any overflow (ao) bit is set, any counter overflow will cause all counters to stop'; 4451-4456 'oper values are 0 for acc, 1 for cnt, and 2 for max'; Table 22 (4461-4482) P0 modes 0-4 and Table 23 (4485-4500) P1 modes 0-4 match
CORRECTED: Same as the claim, with these precision fixes: (a) the SC spec describes FOUR event 'q' groups (q0 read-buffer bits 1:0 [bit0 L2, bit1 SCP rbuf hit], q1 msgsenddata bit 2, q2 tag-bubble bit 3, q3a-d pipeline tag TC: hm 5:4, victim 8:6, vic qwen 13:9, opcodes 14-50); the RTL's three EV defines/3-bit ev_mode fold the bubble into 'tag'. Only 'event mode 0' is defined in the spec. (b) The errata is 3.3 in the TOC (p.77) but its body heading is numbered 4.3 (Errata.txt:2838-2870); the workaround as w

##  | refuted=True | status->needs_fw_change
CLAIM: 
REASON: The RTL-define facts, the firmware default and the mode decode all check out, but the claim is imprecise in four places that matter for the report. (1) STATUS is split, not uniformly needs_fw_change: the default MESHCNT counters (P0 = mesh reads from all sources, P1 = mesh writes, plus the 933 MHz cycle counter) are readable TODAY from a U-mode kernel through SYSCALL_PMC_MS_SAMPLE = SYSCALL_UMODE_THRESHOLD+10 (et-common-libs/include/etsoc/isa/syscall.h:26 @353f20e -> device-minion-runtime/src/WorkerMinion/src/syscall.c:72-73 -> MachineMinion/src/syscall.c:133-134 -> pmu.c:64-81 sample_ms_pmcs; wrapper et_trace_pmc_ms in trace_umode.h:47-51, used by test-compute-kernels/src/pmc/pmc.c:60-64), 
CORRECTED: Each memshire has one performance monitor: a 40-bit cycle counter and two 40-bit event counters P0/P1 shared by its two uMCTL2 controllers (mc0/mc1 select bits), programmed through 8 M-mode-only ESRs at 0x01_Bxy0_0280-02B8 (ESR_DDRC regno 0x10-0x17, PROT=PRV_M). ddrc_perfmon_ctl_status fields: cyc_s/r/ocycl/i bits 0-3, p0_s/r/ocycl/i/e bits 4-8, p0_mode[16:9], p1_s/r/ocycl/i/e bits 17-21, p1_mode[29:22], ao bit 30, cyc/p0/p1 sa/so status bits 32-37; p_e selects event (1) vs resource (0) qualifie

##  | refuted=False | status->needs_fw_change
CLAIM: 
REASON: The core claim survives every check. RTL defines match exactly (external/core-et/rtl/inc/shire_cache_defines.vh:469-490: EV_RBUF/MSG/TAG = 0/1/2; P0 resource modes L2_REQQ/L3_BUSY/ANY_TO_L3/L2_TO_L3/L3_TO_SYS = 0-4; P1 modes L3_REQQ/L2_BUSY/ANY_TO_SYS/L2_TO_SYS/CBUF = 0-4). Shire Cache Spec §4.9 (pdftotext lines 4451-4500) gives oper 0=acc/1=cnt/2=max within a min/max window and Tables 22/23 with exactly the resource lists claimed. Qualifier bit positions confirmed in the firmware HAL at 353f20e (etsoc-hal/include/etsoc_hal/inc/etsoc_shire_cache_esr.h: RD_BUF_LSB 0, MSG_SEND 2, TC_BUBBLE 3, TC_HIT_MISS 4 (mask 0x30), TC_WA_VICTIM 6, TC_QW 9, L2 opcodes 14-25, L3 26-35, SCP 36-41, IDX 42-50; 
CORRECTED: Shire-cache perfmon per the RTL defines (shire_cache_defines.vh:469-490) and Shire Cache Spec §4.9: event mode 0 has FOUR qualifier groups (q0 read-buffer hits, q1 msgsenddata, q2 tag bubble, q3a-d tag-TC: hit/miss bits 5:4, victim type 8:6, victim qwords 13:9, and per-opcode L2 14-25 / L3 26-35 / SCP 36-41 / index-cacheop 42-50 bits); the RTL folds q2+q3 into one EV_TAG source (three event sources RBUF/MSG/TAG). Resource mode P0 {0: active L2/IdxCop reqq entries, 1: L3 reqq busy, 2: outstanding

##  | refuted=False | status->needs_fw_change
CLAIM: 
REASON: Every cited location checks out at commit 353f20e, and the two PVT files are byte-identical between 353f20e and HEAD (`git diff --stat 353f20e HEAD -- device-bootloaders/src/ServiceProcessorBL2/{include/bl2_pvt_controller.h,driver/pvt_controller.c}` is empty). Verified: (1) bl2_pvt_controller.h at 353f20e defines All_PVT_samples {ioshire, pshire, memshire[8], minshire[34]} (lines 328-337), the PVT_PRINT_e per-shire selectors 0..33/232..239/253/254/255 (472-521), per-shire getters pvt_get_min_shire_{ts,pd,vm}_sample / pvt_get_memshire_vm_sample (536-596) and `int pvt_get_and_print(uint8_t print_ts, uint8_t print_vm, PVT_PRINT_e print_select, uint16_t *data, uint32_t *num_bytes)` (647-658), wh
CORRECTED: The 5 Moortec PVT controllers (R_SP_PVT0-4, 0x00_5400_0000+n*0x10000, SP-only per PRM Table 15-37) continuously sample one temperature sensor per minion shire and three voltage-monitor channels per minion shire (vdd_sram, vdd_noc, vdd_mnn), two per memshire (vdd_ms, vdd_noc; no memshire TS), plus IO shire and PShire, with per-sensor current/high/low registers. SP firmware (353f20e = HEAD for these files) has per-shire getters and an export-shaped pvt_get_and_print(..., PVT_PRINT_e, uint16_t *dat

##  | refuted=False | status->works_now
CLAIM: 
REASON: Core claim verified against firmware at 353f20e (identical to HEAD for all PVT files: `git diff --stat 353f20e HEAD` on pvt_controller.c, bl2_pvt_controller.h, thermal_pwr_mgmt.c, dev_mngt_service.cc, device_mgmt_api_rpc_types.h, dm_task.c is empty). Sensor counts/placement: PRM lines 556-580 (5 controllers in IO shire, 36 TS, 36 PD, 8 VM x 16 ch) and bl2_pvt_controller.h lines 20-40, 72-87. Resolutions: PVT_TS_RESOLUTION_RES_12_BITS = 0x0 (hwinc/pvt_ts.h line 17), PVT_VM_RESOLUTION_RES_14_BITS = 0x0 (hwinc/pvt_vm.h line 37); with RES=0 the VM formula's r = 8+(3-0)*2 = 14 so the 3000/(1<<r) term is 0 in integer math, giving range -244..+1220 mV (uint16_t result, so negatives wrap) and TS ran
CORRECTED: On-die PVT: 5 Moortec PVT controllers in the IO shire (SP-only access, PRM Table 15-37) serve 36 temperature sensors, 36 process detectors and 8 voltage monitors x 16 channels (128 sense points), of which the firmware enables only VM0/VM1 per controller (masks 0xFC; PVT4 TS/PD 0xF8, VM 0xFF). Each minion shire has 1 TS, 1 PD and 3 VM channels (vdd_sram, vdd_noc, vdd_mnn); each memshire 2 (vdd_ms, vdd_noc, with memshires 4-7's NoC channel wired to vdd_ms per erratum 5.1); IO shire 3, PShire 2, pl

##  | refuted=True | status->works_now
CLAIM: 
REASON: Most of the claim checks out against the cited sources at commit 353f20e (and the three firmware files are byte-identical at HEAD: `git diff --stat 353f20e HEAD` on pvt_controller.c, bl2_pvt_controller.h, thermal_pwr_mgmt.c is empty). Verified: PRM lines 550-580 (5 controllers, 8 VM x 16 ch = 128 points, Table 1-6); PRM line 11425 R_SP_PVT0 SP-only; TS 12-bit RUN_1, VM 14-bit RUN_0 (bl2_pvt_controller.h lines 92-142); G/H/CAL5/VREF constants (lines 157-172); continuous sampling (pvt_init line 1102 -> pvt_continuous_sample_run lines 538-565); per-channel hardware hi/lo read from alarm_and_hilo[ch].smpl_hilo (pvt_controller.c line 761) and TS smpl_hilo (line 638); integer conversion (pvt_ts_co
CORRECTED: On-die PVT (Moortec IP, 5 controllers in the IO shire, SP-only per PRM Table 15-37): 35 temperature sensors and 35 process detectors (one per minion shire plus one in the IO shire; the PRM's "36" counts a PShire sensor that the RTL says was removed, RTLMIN-3619, and that the firmware masks off), and 8 voltage monitors x 16 channels = 128 sense points, of which the firmware maps 125: 3 per minion shire (vdd_sram, vdd_noc, vdd_mnn), 2 per memshire (vdd_ms, vdd_noc; memshires 4-7 NoC substituted by

##  | refuted=True | status->needs_fw_change
CLAIM: 
REASON: The code-level half of the claim checks out at 353f20e (all files identical to HEAD): et-common-libs/include/etsoc/isa/syscall.h:16-28 ends at SYSCALL_CACHE_OPS_EVICT_WHOLE_L1_L2 = THRESHOLD+11, so +12 is free; syscall_internal.h:4-27 ends at 24, so 25 is free; WorkerMinion/src/syscall.c:24-81 is a flat switch forwarding (arg1,arg2,arg3) via syscall() (S-mode ecall from U-mode dispatched at WorkerMinion/src/trap_handler.S:81-86 with no number range check); MachineMinion/src/syscall.c:65-148 is the M-mode switch reached from trap_handler.S:153-157 for ecall-from-S (only BROADCAST/IPI are fast-pathed, lines 81-84); pmu_core_event_configure is at pmu.h:255-289 (csrw only), pmu_shire_cache_event
CORRECTED: A firmware patch to let a kernel choose PMU events is small (~20-25 lines in four files at 353f20e, identical at HEAD): add SYSCALL_PMC_CONFIGURE (SYSCALL_UMODE_THRESHOLD + 12) in et-common-libs/include/etsoc/isa/syscall.h; SYSCALL_PMC_CONFIGURE_INT 25 in syscall_internal.h; a forwarding case in WorkerMinion/src/syscall.c (S-mode, reached from trap_handler.S:81-86 with no range check); and a case in MachineMinion/src/syscall.c (M-mode, reached from trap_handler.S:153-157) that calls pmu_core_eve

##  | refuted=False | status->works_now
CLAIM: 
REASON: Core claim holds at 353f20e and the lab install, but three details are wrong or imprecise. VERIFIED: header {u64 cycle,u32 payload_size,u16 hart_id,u16 type} packed = 16 B (et-trace/include/et-trace/layout.h:321-326, trace_type_e is uint16_t at :129); stamp = PMC_Get_Current_Cycles() = HPM_SAFE_READ("hpmcounter3") (et-common-libs/src/trace/trace_umode.c:39; pmu.h:386-395,774-779); ET_TRACE_STRING_MAX_SIZE 128 (trace_umode.c:44); entry sizes PMC_COMPUTE 64/SC 32/MS 40/VALUE 32/CUSTOM 24+payload/USER_PROFILE 48 (layout.h:331-369,519-552); syscalls 9/10 dispatched S-mode->M-mode (WorkerMinion/src/syscall.c:69-73 -> MachineMinion/src/syscall.c:130-135 sample_sc_pmcs/sample_ms_pmcs); scounteren/m
CORRECTED: The U-mode trace buffer is the finest software-visible event log on the card today: every entry starts with a 16-byte header {u64 cycle from hpmcounter3 via HPM_SAFE_READ, u32 payload_size, u16 hart_id, u16 type}. The cycle stamp is at minion-clock resolution but comes from one of two free-running per-neighbourhood counters (one shared by all even harts, one by all odd harts of the 8 minions), each reset independently at boot by hart 0 / hart 1 of the neighbourhood, so stamps are comparable only

##  | refuted=True | status->works_now
CLAIM: 
REASON: The core of the claim holds at 353f20e (and HEAD is byte-identical for every cited et-trace / trace_umode / pmu.h / WorkerMinion trace.c+kernel.c / Types.h / KernelLaunch.cpp file: `git diff --quiet 353f20e HEAD` on that set exits 0; only gp-sdk is absent at 353f20e). Verified: 16-byte packed header {u64 cycle,u32 payload_size,u16 hart_id,u16 type} (layout.h:321-326); ET_TRACE_GET_TIMESTAMP()=PMC_Get_Current_Cycles() and ET_TRACE_STRING_MAX_SIZE 128 (trace_umode.c:39,44); HPM_SAFE_READ = .p2align 4 + 4x csrr (pmu.h:386-395) and PMC_Get_Current_Cycles reads hpmcounter3 (pmu.h:774-779); header written after reserve (encoder.h:359-365); wrap-to-header on full (encoder.h:567-569); size_per_hart 
CORRECTED: The U-mode trace buffer is the finest software-visible event log on the card today. Every entry starts with a packed 16-byte header {u64 cycle, u32 payload_size, u16 hart_id, u16 type}; cycle is hpmcounter3 read with HPM_SAFE_READ (4 aligned csrr, RTLMIN-6496 workaround). hpmcounter3 ticks once per minion clock (RTL cycle_up, stopped only in debug) but it is a neighbourhood-shared counter with two copies, one read by all even harts and one by all odd harts, each programmed to CYCLES and zeroed a

##  | refuted=False | status->needs_fw_change
CLAIM: 
REASON: Every cited location says what the claim says at commit 353f20e, and the SP BL2 tree and et-trace/layout.h are identical between 353f20e and HEAD. dm_task.c:215 calls update_pmb_stats(false) every pass; pmic_controller.c:1827-1935 + 376-402 read current/min/max/average for each rail's W_OUT via reg 0x15 after snapshot cmd 0x43, so pmb_stats.<rail>.w_out.current is populated each pass and never consumed. thermal_pwr_mgmt.c:811-843 reads instantaneous board power (reg 0x04) only into CALC_MIN_MAX (macro line 264-266) and copies only w_out.average/min/max into op_stats. op_stats_t is 128 B (op_value packed aligned(8)=8 B x4 x4), emitted at dm_task.c:301-313. Units confirmed (rail mW per et-top.
CORRECTED: The SP stats trace record could carry instantaneous rather than averaged power with a few-line SP firmware change (~4 assignments in update_module_soc_power): every ~133 ms pass the SP already reads each rail's PMB W_OUT 'current' value into g_pmic_power_reg.pmb_stats.<rail>.w_out.current and instantaneous board power into g_pmic_power_reg.soc_pwr_10mW, but copies only w_out.average/min/max into op_stats and uses the board value only for min/max. Writing the instantaneous values into the min or 

##  | refuted=False | status->impossible_on_silicon
CLAIM: 
REASON: Every cited location says what the claim says, at commit 353f20e. (1) et-trace/include/et-trace/layout.h lines 465-490 at 353f20e: `struct op_value { uint16_t avg; uint16_t min; uint16_t max; }` and op_module/op_stats_t with minion/sram/noc/system power; identical at HEAD (same line numbers). (2) device-management-application/src/et-top.cc lines 792-796 at 353f20e: CARD displayed with OP_VALUE_UNIT_POWER_10MILLIWATTS, MINION/SRAM/NOC with OP_VALUE_UNIT_POWER_MILLIWATTS, so rail LSB = 1 mW and board LSB = 10 mW, giving 1 mW x 133 ms = 133 uJ and 10 mW x 133 ms = 1.33 mJ as claimed. (3) docs/research/power-telemetry.md lines 98-101 says verbatim "None. Neither the PRM, the firmware nor the Erb
CORRECTED: Tracking individual bit flips through power is impossible on silicon. The finest energy quantum any sensor path on the card reports is one rail-power LSB over one SP stats record: 1 mW x 133 ms = 133 uJ for the minion/SRAM/NoC rails (board: 10 mW LSB, 1.3 mJ; the PMIC register template even suggests 0.25 W). The record fields are the PMIC's running average/min/max of W_OUT, not instantaneous samples, so the effective resolution is coarser still. A single node toggle at the 0.5 V minion supply wi

##  | refuted=True | status->needs_fw_change
CLAIM: 
REASON: The measured numbers, transaction count, record size, ring capacity and status (needs_fw_change) all check out, but the claim's causal story and its conclusion are wrong. (1) The ~1.2 ms per I2C transaction is not "clock stretching or command processing in the ATSAMD20 PMIC": the SP I2C driver at 353f20e (unchanged at HEAD) ends EVERY i2c_read and i2c_write with a hard-coded 1 ms busy-wait, external/et-platform device-bootloaders/src/ServiceProcessorBL2/driver/i2c_controller.c lines 237-239 and 318-320: "/* TODO: Fix I2C driver to use ack instead of adding delays after each operation delays are added to prevent SP from hanging with back to back I2C read/writes. */ US_DELAY_GENERIC(1000)". A 
CORRECTED: The SP sampling loop runs once per 133.6 ms (median of 2,147 trace records, 132.9-135.2 ms), not the 10 ms DM_TASK_DELAY_MS suggests. The pass is dominated by 96 PMIC I2C transactions (1 snapshot write + 84 PMB stat reads + 9 voltage reads + average and instantaneous board power) at ~1.16 ms each; that cost is a firmware artifact, not a PMIC limit: the SP I2C driver ends every read and write with a hard-coded 1 ms busy-wait (i2c_controller.c lines 237-239, 318-320, "delays are added to prevent S

##  | refuted=True | status->needs_fw_change
CLAIM: 
REASON: The measured numbers and every cited line check out at 353f20e (and are unchanged at HEAD 836a4ab60), but the claim's mechanism and its conclusion are contradicted by the same firmware. (1) The ~1.2 ms per PMIC transaction is NOT "clock stretching or command processing in the PMIC": the SP I2C driver at 353f20e ends every i2c_read() and i2c_write() with a hard-coded busy-wait `US_DELAY_GENERIC(1000)` (device-bootloaders/src/ServiceProcessorBL2/driver/i2c_controller.c lines 237-239 and 318-320, comment: "TODO: Fix I2C driver to use ack instead of adding delays after each operation / delays are added to prevent SP from hanging with back to back I2C read/writes"). delays.h lines 13-18 only shri
CORRECTED: The SP sampling loop runs once per 133.6 ms (median of SP-stats-trace timestamps, 5954 records), not the 10 ms DM_TASK_DELAY_MS suggests. Each pass makes 96 PMIC I2C transactions (1 PMB snapshot write + 84 PMB stat reads + 9 rail-voltage reads + average and instantaneous board power), and the dominant cost is the SP I2C driver itself: i2c_read()/i2c_write() at 353f20e end every transaction with a hard-coded 1000 us busy-wait (i2c_controller.c lines 239 and 320, production build FAST_BOOT=0), i.e

##  | refuted=True | status->needs_tooling
CLAIM: 
REASON: The code-level part of the claim checks out: at 353f20e (identical to HEAD for all six files) there is no PMC-configure syscall (U-mode ids 1-11 in et-common-libs/include/etsoc/isa/syscall.h:17-27, internal ids 1-24 in device-minion-runtime/src/shared/minion_rt_helpers/include/syscall_internal.h:5-28, so 12 and 25 are free); WorkerMinion/src/trap_handler.S:80-86 forwards every U-mode ecall to syscall_handler; the S->M `syscall()` helper is syscall.h:50-61; MachineMinion/src/trap_handler.S:75-83,150-156 fast-paths only IPI/BROADCAST and sends everything else to the C handler; pmu_core_event_configure (pmu.h:255-292) uses csrw only, which is the write-only form the erratum allows (Errata body 
CORRECTED: Letting a kernel pick events by syscall is a small code change (free ids 12/25, existing csrw helpers, all dispatch paths present at 353f20e), but deploying it is not "rebuild and -o 15": the minion images are signed Esperanto executable images that BL2 verifies (certificate, PK signature, code+data hash) unless the OTP chicken bit is set; the signing tool, test certificates and signed images the flash template references are not in the drop or in /opt/et; a whole 2 MB partition with every other

##  | refuted=False | status->impossible_on_silicon
CLAIM: 
REASON: Every cited fact checks out at et-platform 353f20e and in the manuals, and nothing in the repo offers a finer energy path. (1) The only per-rail energy data that reaches the host is op_stats_t: struct op_value {uint16 avg,min,max} per rail (et-trace/include/et-trace/layout.h lines 465-469 at 353f20e), filled from PMB w_out.average/min/max only (ServiceProcessorBL2/services/thermal_pwr_mgmt.c ~lines 833-843: op_stats.minion/noc/sram.power.{avg,min,max} = pmb_stats.*.w_out.*); a_out/a_in/w_in are read by pmic_get_pmb_stats but never forwarded. Units are mW (et-top.cc lines 794-796 at 353f20e, OP_VALUE_UNIT_POWER_MILLIWATTS; system power in 10 mW). (2) The 133 ms record period is a measured val
CORRECTED: 

##  | refuted=True | status->needs_tooling
CLAIM: 
REASON: Three parts of the claim do not survive the code at 353f20e (which is byte-identical to HEAD for pvt_controller.c, bl2_pvt_controller.h, thermal_pwr_mgmt.c, trace.c, log.c, dm_task.c and device_mgmt_api_spec.h: `git diff --stat 353f20e HEAD` on those paths is empty).

(1) "A per-shire map exists in the SP every loop pass" is imprecise. Nothing in SP RAM holds a per-shire array. Every getter (pvt_get_min_shire_ts_sample, driver/pvt_controller.c:645-657; pvt_get_min_shire_vm_sample, :732-807) reads the PVTC hardware registers (sdif_data + alarm_and_hilo[].smpl_hilo) at call time, and the loop consumers reduce immediately to avg/high/low (GET_MINION_VM macro :25-91, pvt_get_minion_avg_low_high_
CORRECTED: The per-shire PVT map (34 minion shires x {T, PD, V_sram, V_noc, V_mnn}, 8 memshires x {V_ms, V_noc}, IO shire, PShire; each with current plus hardware high/low since last reset) lives in the five SP-only Moortec PVT controller register blocks, which sample continuously. The SP firmware reads every shire once per ~133 ms loop pass but keeps only avg/high/low. Per-shire VOLTAGES (with hi/lo) are nevertheless exported today as DEBUG log strings: DM_CMD_SET_DM_TRACE_CONFIG (67) with event_mask=TRAC

##  | refuted=False | status->needs_fw_change
CLAIM: 
REASON: The mechanism is verified at 353f20e (and identical at HEAD: `git diff 353f20e HEAD --stat` on the five files is empty). dm_task.c:215 calls update_pmb_stats(false) every pass; thermal_pwr_mgmt.c:1334-1356 stores the full pmb_stats_t (incl. w_out.current) in g_pmic_power_reg; pmic_controller.c:376-400 reads current/min/max/average per quantity (4-byte reads masked 0xffff); thermal_pwr_mgmt.c:811-843 writes system.power.avg from the PMIC average register (0x29), CALC_MIN_MAX from the instantaneous register (0x8), and copies only w_out.average/min/max for the three rails. op_stats_t is 4x32 B = 128 B (layout.h:465-490); dm_task.c:301-313 emits it as TRACE_CUSTOM_TYPE_SP_OP_STATS. Host decoders
CORRECTED: The SP stats trace could carry the PMIC's snapshot ("current") W_OUT per rail and the raw input_power board value instead of/in addition to the PMIC moving average with a few-line change to update_module_soc_power() (thermal_pwr_mgmt.c:821-843), keeping the 128-byte op_stats_t layout by repurposing the min or max slots (only et-top's min/max display and DM_CMD_GET_SP_STATS semantics change; throttling uses .avg only). Caveats: the lab card requires flashing a BL2 image that BL1 signature-checks 

##  | refuted=False | status->needs_fw_change
CLAIM: 
REASON: Every cited location says what the claim says at commit 353f20e, and the files are byte-identical at HEAD 836a4ab (git diff --stat empty for mem_controller.c, memshire_ddr_init_functions.c, historical_extreme.c, ddrc_reg_def.h, device_mgmt_api_spec.h). Verified: (1) device-bootloaders/src/ServiceProcessorBL2/driver/mem_controller.c:643-647 hard-codes `.ecc = false` with a `//TODO: decide ddr_mode` comment (line 649); line 240 maps it to `config_ecc = ddr_mode->ecc ? 1 : 0`. (2) etsoc-hal/src/memshire_ddr_init_functions.c:4551-4561 writes ECCCFG0=0x003f7f14 (ecc_mode=4) when config_ecc, else 0x003f7f10 (ecc_mode=0); DWC_ddr_umctl2.h:2456-2458 puts ecc_mode at bits [2:0], and DWC_ddr_umctl2.ht
CORRECTED: DRAM ECC is disabled on the lab card: SP BL2 hard-codes ddr_mode.ecc=false (mem_controller.c:645, with a TODO at :649), which makes the HAL program ECCCFG0=0x003f7f10 (ecc_mode=0, memshire_ddr_init_functions.c:4557); unchanged at HEAD 836a4ab. The uMCTL2 ECC interrupts (ECCCTL=0x300 enabled at HAL:4566, ESR/PLIC enables at mem_controller.c:458-488) therefore never fire, and DM_CMD_GET_MODULE_DDR_ECC_UECC (47, link_mgmt.c:288-315) and DM_CMD_GET_MAX_MEMORY_ERROR (40, historical_extreme.c:37-60) r

##  | refuted=True | status->needs_fw_change
CLAIM: 
REASON: The cited registers and firmware routines exist and behave as quoted at 353f20e (paths corrected: ServiceProcessorBL2/driver/minion_configuration.c 380-425, 670-720, 1110-1160; thermal_pwr_mgmt.c 1761; PRM 15287 and 15312-15314; no diff vs HEAD). But three load-bearing statements are unsupported or wrong. (1) Access: the only existing code path is Service Processor BL2 writing shire ESRs over the NoC (SWITCH_CLOCK_MUX = write_esr_new(PP_MACHINE, shire_id, ...), etsoc-hal/include/hwinc/lvdpll_defines.h 106-108); there is no M-mode minion LVDPLL code at 353f20e, so 'mmode_firmware' is a mislabel and the change is an SP-BL2 flash. (2) 'Possible in hardware' is not established on A0 silicon: Err
CORRECTED: Per-shire clocking is architecturally present (per-shire LVDPLL ESRs shire_pll_auto_config/shire_ctrl_clockmux, M-privilege, PRM 15287-15314) and the SP-BL2 firmware already loops per shire, but the shipped firmware puts every active shire on the chip-wide PLL4 step clock and disables its LVDPLL (Minion_Configure_Hpdpll 712-713), and Errata 7.3/FV-120 reports the LVDPLL as not stable and Not Fixed on A0, so running one shire at a different LVDPLL frequency is untested on silicon. An SP-BL2 chang

##  | refuted=False | status->impossible_on_silicon
CLAIM: 
REASON: The claim survives every check I could make against the open sources. (1) Datasheet §2.1.1.5 (ET Preliminary Datasheet Rev 1.0.txt:342-349) describes the 4 KB D$ as 4 LRAM blocks of 128 rows x 64 bits, with no ECC statement; the only 'parity' mentions in the whole datasheet are the UART (lines 1257-1258). (2) In the Erbium RTL (core-et b38a1a3), `grep -rniE 'ecc|parity|secded'` over rtl/shire/minion/dcache/ returns nothing; rtl/inc/dcache_defines.vh:52 sets DCACHE_LRAM_NUM_BANKS 4 and dcache_data_array.v:119-135 instantiates 4 dcache_data_ram_wrap, each of which (dcache_data_ram_wrap.v:26-52) is two dcache_128x32_1r1w_lram macros with a bare 32-bit data port and no check bits or error output
CORRECTED: Minor precision only: each of the 4 D$ '128x64' blocks is implemented as two 128x32 LRAM macros (8 physical macros, dcache_data_ram_wrap.v:26-52); the rtl/libs/macros/*.v files are Ainekko-2026 behavioral stand-ins for the foundry hard macros, so the strongest statement is 'no ECC/parity bits, ports, or error outputs exist at the macro boundary or anywhere in the D$/RF logic', which still implies no hardware detection path. The D$ tag/metadata array and the tensor register files (vpu_tensor{a,b,

##  | refuted=False | status->impossible_on_silicon
CLAIM: 
REASON: Every cited location says what the claim says, and independent checks corroborate it. (1) Datasheet §2.1.1.5 (ET Preliminary Datasheet Rev 1.0.txt:342-349) states the D$ is 4 LRAM blocks of 128 rows x 64 bits; RTL matches: dcache_defines.vh:52 `DCACHE_LRAM_NUM_BANKS 4`, and each block is dcache_data_ram_wrap.v (two dcache_128x32_1r1w_lram halves). The macro rtl/libs/macros/dcache_128x32_1r1w_lram.v is a plain `reg [31:0] mem[0:127]` with no check bits. (2) `grep -rilE 'ecc|parity|secded' rtl/shire/minion/dcache/` returns nothing; dcache_data_array.v/dcache_metadata_array.v have no err/fault outputs; `grep -rilE 'parity|secded' rtl/shire` returns nothing at all. (3) The only ECC hits under rt
CORRECTED: The 4 KB L1 D$ (and the 3 KB L1 tensor scratchpad carved from it), the latch-based integer register file (rf_latch_2r_1w) and the 8x64x32-bit VPU register file (vpu_64x32_3r2w_vpurf) have no ECC and no parity. The D$ is four 128x64 LRAM banks, each built from two 128x32 latch-RAM halves; the Erbium minion RTL contains no ECC/parity logic outside the fetch path (I$ ecc_err -> FETCH_ECC_ERROR exception 26 / MIECO interrupt 19), and the PRM lists no D$ or register-file error ESR, CSR bit or excepti

##  | refuted=True | status->needs_fw_change
CLAIM: 
REASON: The core facts hold, but two load-bearing sub-claims are wrong, so the claim as written is refuted-in-part. VERIFIED at 353f20e: configure_memshire() hard-codes `.ecc = false` (device-bootloaders/src/ServiceProcessorBL2/driver/mem_controller.c:643-647); ddr_config sets config_ecc = ddr_mode->ecc (line 240) and the HAL writes ECCCFG0=0x003f7f10 (ecc_mode=0) vs 0x003f7f14 (ecc_mode=4) at etsoc-hal/src/memshire_ddr_init_functions.c:4551-4560; ECCCTL=0x300 (CE+UE intr enable, line 4566). The SP does enable both interrupts at boot (event_handler.c:79 -> ddr_error_control_init, mem_controller.c:437-490: ddrc_normal_int_en |= 0x3, ddrc_critical_int_en |= 0x180) but with ecc_mode=0 they never fire, 
CORRECTED: DRAM ECC is disabled on the lab card: the SP BL2 hard-codes ddr_mode.ecc=false (mem_controller.c:643-647), which makes the HAL program ECCCFG0=0x003f7f10 (ecc_mode=0) instead of 0x003f7f14 (ecc_mode=4, SECDED inline ECC, ecc_region_map=0x7f). The SP still enables the DDRC CE/UE interrupts at boot (event_handler.c:79 -> ddr_error_control_init), but with ecc_mode=0 they never fire, so the software counters returned by DM_CMD_GET_MODULE_DDR_ECC_UECC (47, link_mgmt.c:290-320) and DM_CMD_GET_MAX_MEMO

##  | refuted=True | status->needs_fw_change
CLAIM: 
REASON: The mechanism part of the claim survives: every shire has its own LVDPLL and a 3-way clock mux (PRM lines 15287-15306, 15316-15318, M-mode; RTL dv/soc/shire/shire_top_gate_wrap.v ports ref_clock 100 MHz, step_clock 1 GHz, clk__noc; rtl/inc/shire_defines.vh lines 428-445), the SP firmware at 353f20e applies ONE mode to every masked shire (device-bootloaders/src/ServiceProcessorBL2/driver/minion_configuration.c lines 379-425 minion_configure_plls_and_dlls(shire_mask, mode); lines 670-720 Minion_Configure_Hpdpll: SWITCH_CLOCK_MUX(shireid, SELECT_STEP_CLOCK); lvdpll_disable(shireid)), boot uses the step clock (common/main.c lines 94-97, 403-414; include/minion_configuration.h line 82 MINION_PLL_
CORRECTED: Per-shire frequency exists in hardware (per-shire LVDPLL plus a per-shire M-mode clock mux with ref 100 MHz / PLL4 step clock / LVDPLL inputs) but is not reachable from the host: the SP firmware at 353f20e applies one mode to all active shires, boots and runs DVFS on the chip-wide PLL4 step clock, the DM_CMD_SET_FREQUENCY command has no shire mask (though its handler does accept use_step_clock=FALSE for an all-shire LVDPLL mode), and MDI writes cannot reach ESRs. Making one shire run at a differ

##  | refuted=True | status->needs_fw_change
CLAIM: 
REASON: Most cited locations say what the claim says (PRM 14938-14948 register list; HAL field positions; BL2 ISR + threshold 32; driver parse/sysfs; dev_mngt_service case; errata text; no diff 353f20e->HEAD in any cited file). But the headline "wired all the way to the host today / works_now" is false at 353f20e (and at HEAD): the SP never enables the SPIO-PLIC MINSHIRE_ERR0..33 interrupt sources. sram_error_control_init() (bl2_cache_control.c:225-239, called from event_handler.c:82 <- main.c:523) only sets the 5 interrupt-enable bits in each bank's sc_err_log_ctl ESR. The PLIC-side enable + vector install, INT_enableInterrupt(SPIO_PLIC_MINSHIRE_ERR0_INTR+minshire, 1, sram_and_icache_error_isr), li
CORRECTED: The shire-cache SRAM (L2/L3/SCP banks) has per-bank ECC error loggers (sc_err_log_ctl/info/address, M-mode ESRs at 0x01_C030_x058..x068) and saturating SBE/DBE counters (sc_sbe_dbe_counts, x070; 8-bit SBE, 3-bit DBE per RAM class). sc_err_log_info (ECC format) carries valid, multiple, enabled, imprecise, code[7:4], index[39:8], error_bits[47:40] and ram[51:48]; error_bits is a multi-hot mask of which 64-bit doubleword(s) in the line had an error (one SECDED code per doubleword), not the bit posi

##  | refuted=True | status->needs_tooling
CLAIM: 
REASON: The core mechanism is correctly cited and present in the lab firmware, but three load-bearing statements are false or misread at commit 353f20e. (1) "single-step available": DM_CMD_MDI_ENABLE/DISABLE_SINGLE_STEP are stubs that return status -1 with the comment "Not supported in MDI lib / SW-11247" (device-bootloaders/src/ServiceProcessorBL2/services/minion_debug.c:447-469); minion_run_control.c has no step function (only Halt_Harts, Resume_Harts, Select/Unselect, Set_PC, Set_PC_Breakpoint, lines 12-53). DM_CMD_MDI_RESET_HART is likewise a stub ("MDI lib does not support reset hart functionality yet", minion_debug.c:346-355). (2) "hart-id range checks are compiled out under MDI_PRIVILEDGED_AC
CORRECTED: The Minion Debug Interface (MDI) is the finest architectural-state read on silicon and is compiled into every ServiceProcessorBL2 variant at et-platform 353f20e (CMakeLists.txt:105,124,151; dispatched at command_dispatcher.c:187). Working DM commands: SELECT/UNSELECT/HALT/RESUME hart, GET_HART_STATUS, SET/UNSET PC-breakpoint, READ/DUMP/WRITE_GPR, READ/WRITE_CSR, READ/WRITE_MEM. RESET_HART and ENABLE/DISABLE_SINGLE_STEP are stubs returning -1 ("Not supported in MDI lib", SW-11247), so there is no

##  | refuted=True | status->needs_fw_change
CLAIM: 
REASON: The register/ISR/driver/tool chain the scout describes exists in source, but it is not live on the card: the service processor never enables the PLIC interrupt sources that carry shire-cache (and icache) error interrupts, so the ISR never runs and every downstream count/event is permanently zero. At et-platform 353f20e, dm_event_control_init() (rtos_task/event_handler.c:82, called from common/main.c:523) calls sram_error_control_init() (driver/bl2_cache_control.c:304-317), which only sets the per-bank ESR interrupt-enable bits in sc_err_log_ctl via sc_err_interrupt_enable(). The SP-side PLIC enable + ISR registration lives solely in enable_sram_and_icache_interrupts() (driver/minion_configur
CORRECTED: The shire-cache SRAM has per-bank ECC error loggers (sc_err_log_ctl/info/address) and hardware SBE/DBE counters (sc_sbe_dbe_counts), all M-mode-only ESRs, and the SP firmware, driver and dev_mngt_service contain a complete reporting path for them (ISR -> SRAM_CE/SRAM_UCE event with ERR_LOG_INFO/ADDRESS syndrome -> dmesg decode + sysfs counts; DM_CMD_GET_MODULE_SRAM_ECC_UECC). However at the lab firmware (et-platform 353f20e, and still at HEAD) the SP sets the ESR-level interrupt enables but neve

##  | refuted=True | status->works_now
CLAIM: 
REASON: The substance survives (sys_emu -l is a per-instruction architectural-state trace; status works_now on the installed binary), but three cited specifics are wrong at 353f20e and one quote is partly fabricated, so the claim as stated is imprecise. (1) The "Fetched instruction from PC 0x…: 0x<encoding>" line does NOT exist at 353f20e: both LOG_HART sites are commented out (sw-sysemu/mmu.cpp:403 and :413), and `strings /opt/et/bin/sys_emu` has no "Fetched instruction" string. The README:63-69 example is stale. What is actually printed is a single line per instruction `I(<prv>): 0x<pc> (0x<bits>) <mnemonic operands>` (sw-sysemu/insn_util.h:655-746, e.g. line 686 for addi; installed binary strings
CORRECTED: sys_emu -l (installed /opt/et/bin/sys_emu, an ET-SoC-1 non-SDK_RELEASE build whose option set matches et-platform 353f20e exactly except the SYSEMU_PROFILING-gated -dump_prof) is a per-instruction architectural-state trace: for every executed instruction on every selected hart in any privilege mode it prints one line `I(<M|S|U>): 0x<pc> (0x<encoding>) <disassembly>` (insn_util.h:655-746; the README's "Fetched instruction from PC" line is stale, commented out in mmu.cpp:403/413), followed by each

##  | refuted=False | status->needs_fw_change
CLAIM: 
REASON: Every cited location says what the claim says, and the claim holds at the lab-card commit 353f20e as well as HEAD. (1) PRM Table 15-81, "ET Programmer's Reference Manual.txt" lines 14910-14937: sc_idx_cop_sm_physical_index (0x01_C030_x038) "defines the physical index into the logical RAM for Dbg_Read/Dbg_Write operations ... sub-bank select, sub-bank way, physical set, and quadword"; data0/data1 (x040/x048) hold Dbg_Read read data / Dbg_Write write data; sc_idx_cop_sm_ecc (x050) "holds the read ECC of the Dbg_Read operation or the write ECC to use for the Dbg_Write operation (if ecc_wr_en = 1)". All are privilege M. (2) et-platform 353f20e device-minion-runtime/src/MachineMinion/include/shir
CORRECTED: The shire cache's M-mode index-cacheop state machine (sc_idx_cop_sm_ctl, PRM Table 15-81 at 0x01_C030_x030) has opcodes 7=Dbg_Read and 8=Dbg_Write (shire_cache.h:29-30 at 353f20e; shire_cache_types.vh:226-227). ctl fields (etsoc_shire_cache_esr.h at 353f20e): go bit 0, opcode 11:8, ecc_wr_en bit 13, logical_ram 15:14 (Tag_State / Tag_RAM / Data_RAM), state 31:24 (IDLE=0x04). sc_idx_cop_sm_physical_index selects qw 1:0, sub-bank 10:8, way 17:16, physical set 47:32. For Data_RAM one transaction mo

##  | refuted=True | status->needs_tooling
CLAIM: 
REASON: The mechanism and the plumbing are real and reachable from a user account (SP dispatches DM_CMD_MDI_SELECT_HART..WRITE_MEM at command_dispatcher.c:187-190; minion_debug.c/MDI lib are compiled into ALL five BL2 variants incl. production, CMakeLists.txt:105,131; et-driver never filters msg_id on the mgmt SQ, only special-cases events and MM_RESET in et_vqueue.c:1079-1080,1137; /dev/et*_mgmt is mode 0666 (udev-50-et.rules:1, et-soc1-pcie.c:2133) and on aifoundry2 `ls -l` shows crw-rw-rw- /dev/et0_mgmt; libDM serviceRequest memcpy-passes MDI payloads, DeviceManagement.cc:521-541; no commits after 353f20e touch these paths). But five specific assertions in the claim are wrong at 353f20e: (1) sing
CORRECTED: The Minion Debug Interface (MDI) is the finest architectural-state read available on silicon and is compiled into every BL2 variant at 353f20e, including production. The SP handles DM commands 128-144; the mgmt device node is 0666 and neither et-driver nor libDM filters MDI msg_ids, so a user-account libDM client can drive it (in-tree gtest clients exist under devicemanagement/tests/mdi_debug but are not installed in /opt/et/bin). What actually works: SELECT/UNSELECT whole neighbourhoods (16 har

##  | refuted=True | status->works_now
CLAIM: 
REASON: The substance (sys_emu -l gives an instruction-granularity trace of architectural writes, usable today) holds, but the claim misdescribes the format and overstates completeness. (1) There is no 'Fetched instruction from PC 0x…: 0x<encoding>' line at 353f20e: both LOG_HART calls are commented out in sw-sysemu/mmu.cpp:396,403,409,413 and the string is absent from /opt/et/bin/sys_emu (strings count 0); PC and encoding appear instead inside the disassembly line 'I(<prv>): 0x<pc> (0x<bits>) <mnemonic> …' (insn_util.h:654-729; binary contains 'I(%c): 0x%lx (0x%08x) addi x%d,x%d,%ld'). The README example (README.md:63-69) is stale. (2) 'Each state change' is not logged: trap entry emits only 'Trapp
CORRECTED: sys_emu -l (installed /opt/et/bin/sys_emu is a non-SDK, non-profiling build of et-platform 353f20e) prints, for every executed instruction on every logged hart, one line '<emu_cycle>: DEBUG EMU: [H<n> S<s>:N<n>:C<c>:T<t>] I(<prv>): 0x<pc> (0x<encoding>) <disasm>' followed by the architectural writes that instruction performs: 'xN = 0x…' (x0 skipped), 'fN = { 0:… 7:… }' for the 8 VPU lanes, 'cN' TenC lanes, 'mN' mask regs, 'SCP[row]'/'TenB[row]' scratchpad lines, 'fflags', '<csr> : old' / '<csr> 

##  | refuted=False | status->needs_fw_change
CLAIM: 
REASON: The claim survives every check. (1) Registers and opcodes are real: PRM Table 15-81 (PRM.txt:14918-14937) documents sc_idx_cop_sm_physical_index/data0/data1/ecc with Dbg_Read/Dbg_Write semantics; firmware header shire_cache.h:29-30 at 353f20e defines DBG_READ=7/DBG_WRITE=8; RTL enum core-et rtl/inc/shire_cache_types.vh:226-227 (SC_IdxCopSm_Dbg_Read=4'd7, Dbg_Write=4'd8). HAL etsoc_shire_cache_esr.h at 353f20e gives the exact fields: physical_index.sbank[10:8], way[17:16], physical_set[47:32], qw[1:0]; ctl.logical_ram[15:14], ctl.ecc_wr_en[13], ctl.opcode[11:8]; ecc.Data_RAM dw0_ecc[7:0], dw1_ecc[15:8] (8 ECC bits per 64-bit doubleword, SC_DW_ECC_SIZE=8 in shire_cache_defines.vh:250). (2) Sta
CORRECTED: Raw shire-cache SRAM rows can be read and written with their ECC check bits via the M-mode index-cacheop state machine (opcode 7 Dbg_Read / 8 Dbg_Write): sc_idx_cop_sm_ctl.logical_ram selects Data_RAM, Tag_RAM or Tag_State; sc_idx_cop_sm_physical_index selects sub-bank[10:8], way[17:16], physical set[47:32] and quadword[1:0]; data lands in sc_idx_cop_sm_data0/1 and the check bits in sc_idx_cop_sm_ecc (8 bits per 64-bit doubleword for data, 6 per tag, 7 for tag state), with ecc_wr_en allowing a d

##  | refuted=True | status->works_now
CLAIM: 
REASON: The inventory itself is accurately transcribed from csr.csv and the two PRM discrepancies (minstmatch 0x7CC vs 0x7CE; mcause 0x1C vs 30) are real and confirmed by three independent sources (RTL csv line 140, HAL minion_csr.h:297 at 353f20e, sw-sysemu/csrs.h:242; PRM lines 329/1201 say cause 30). But the claim presents an ERBIUM CSR inventory as the ET-SoC-1 inventory. docs/et-soc1-notes.md:284 states core-et's erbium branch is a different MCU-class SoC. The ET-SoC-1 golden model at the lab-card commit (sw-sysemu/csrs.h 353f20e lines 239-293) and the ET-SoC-1 HAL (etsoc-hal/include/etsoc_hal/inc/minion_csr.h 353f20e) both contain 13 user-level custom CSRs that csr.csv does not: tex_send 0x807
CORRECTED: Custom CSR inventory (ET-SoC-1, from sw-sysemu/csrs.h and etsoc-hal minion_csr.h at et-platform 353f20e, cross-checked against the Erbium-branch RTL csr.csv): 37 user-level Esperanto CSRs = tensor_reduce/fma/conv_size/conv_ctrl/coop/mask/quant 0x800-0x806, tex_send 0x807, tensor_error 0x808, ucache_control 0x810, co_prefetch_va 0x81F, flb 0x820, fcc 0x821, stall 0x822, tensor_wait 0x830, tensor_load 0x83F, gsc_progress 0x840, tensor_load_l2 0x85F, tensor_store 0x87F, co_evict/flush/lock/unlock_v

##  | refuted=True | status->works_now
CLAIM: 
REASON: The core of the claim survives: PRM §2.4 Table 2-2 (lines 891-1150) does give bit-field encodings for ~205 PS/PI/Mask/Atomic mnemonics, and the installed binutils 2.45 assembles and disassembles them and names the custom CSRs exactly as the scout reports (verified by assembling and disassembling a test file: flw.ps/fmadd.ps/fbc.ps/fbcx.ps/fbci.ps/flq2/fswizz.ps/fadd.pi/maskand/mova.x.m/amoaddl.w/amoaddg.d/flwl.ps/sbl/fcvt.ps.f16/fsin.ps/cubeface.ps/bitmixb/packb/fcmovm.ps plus csr names evict_va 0x89F, tensor_wait 0x830, hartid 0xCD0, flb 0x820, tensor_fma 0x801, tensor_load 0x83F, tensor_load_l2 0x85F, hpmcounter3 0xC03, flush_va, prefetch_va, lock_va, unlock_va, evict_sw 0x7F9, fcc 0x821, 
CORRECTED: The ISA is documented at the bit level and decoded by name by the installed toolchain, with caveats. PRM §2.4 Table 2-2 (lines 891-1150) gives 32-bit field encodings for ~205 Packed-Single, Packed-Integer, Mask and Atomic instructions; four Atomic compare-and-swap ops (AMOCMPSWAP{L,G}.{W,D}) are encoded only in the per-instruction pages of §7.2, and Table 2-2's "MASKPOPZ" is MASKPOPCZ everywhere else. Tensor, cache-control, FLB and FCC ops are CSR writes (standard csrrw encodings) whose data-wor

##  | refuted=True | status->works_now
CLAIM: 
REASON: The substantive facts survive (RTL addresses, PRM omissions, the 0x7CC-vs-0x7CE and 0x1C-vs-30 discrepancies are each confirmed by 4 independent chip-side sources: Erbium csr.csv/csr_types_auto.vh, the Semifore spec in core-et, etsoc-hal minion_csr.h at 353f20e, and sw-sysemu at 353f20e), but the claim as stated is imprecise in ways a reviewer would catch: (1) cited line ranges are wrong (csr.csv M-mode customs are lines 136-156, not 155-181, which are hardwired hpmcounters; main.c menable_shadows write is line 131, not 129, at both 353f20e and HEAD); (2) '14 implemented M-mode' contradicts its own 15-name list, and csr.csv has 15 implemented=YES (ddata0 is filed under Debug Mode Registers; 
CORRECTED: Custom CSR inventory. The ET-SoC-1 CSR map (etsoc-hal/include/etsoc_hal/inc/minion_csr.h at et-platform 353f20e, generated from the Semifore spec that core-et ships at rtl/shire/minion/intpipe/csr/semifore/minion_csr.csr) has 37 user-level Esperanto CSRs: tensor_reduce/fma/conv_size/conv_ctrl/cooperation/mask/quant 0x800-0x806, tex_send 0x807, tensor_error 0x808, ucache_control 0x810, co_prefetch_va 0x81F, flb 0x820, fcc 0x821, stall 0x822, tensor_wait 0x830, tensor_load 0x83F, gsc_progress 0x84
