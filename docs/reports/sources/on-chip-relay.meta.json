{"title": "Hand it to the next shire",
 "description": "A multi-stage computation that passes each stage's output to a neighbouring shire's scratchpad instead of writing it to DRAM runs 12 times faster and uses 12 times less energy per byte. Measured on two ET-SoC-1 cards, with the boundary where the advantage appears and the one where it runs out."}
